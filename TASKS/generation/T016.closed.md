STATUS: DONE

# T016 Acceptance - Fixed-Panel Optimizer Consumer API

## Decision

Accepted.

## Verified

- `optimizeFormation` accepts optional `OptimizeFormationOptions.opponents`.
- Omitting the panel preserves the existing `FORMATION_LIBRARY` default behavior.
- Supplying a panel constrains initial traces, sample/diagnostic processing, matched-opponent filtering, branch search, and independent validation to that panel.
- Empty panels fail before simulation.
- The canonical generation panel of bundle first seven plus `壕炸金猴` is supported and auditable through diagnostics.
- Existing tree decision logic, search operators, validation threshold, experience semantics, active formation data, and deployment artifacts were not changed.
- Executor evidence reports 5/5 focused checks and 33/33 regression checks.

## Verification Note

A local direct run of the real-simulation panel test exceeded the 120-second harness command limit during the custom two-panel case. It produced no assertion failure before timeout. The executor's bounded test report, source audit, and protected-artifact checks are accepted as the delivery evidence; T019 will use the API in the bounded candidate-level runner rather than rerun this expensive standalone scenario.

## Follow-up

T019 will consume the authoritative T017 frozen pool through the T018 candidate-level parallel runner.
