STATUS: DONE

# T009 Acceptance - Diversity-Aware Candidate Retention

## Decision

Accepted after T010 restored the audited T008 evaluated dataset and isolated test outputs.

## Verified

- `npx vite-node tests/candidate_retention.test.ts` passes 9/9.
- The production retention analysis reads six evaluated candidates, retains four, and rejects two zero-score candidates.
- All retained candidates use a valid performance source: refined or coarse, never `none`.
- The retained pool respects the user-selected limits: maximum six records and exploration floor `0.25`.
- The performance baseline is `cand_halfrush_1_2fa134` at 92.9% refined AD.
- Archetype coverage retains prayer and fullrush representatives; the fourth retained candidate adds a distinct halfrush/module direction.
- The two 0% coarse candidates are rejected despite high novelty, preserving the exploration floor.
- The novelty formula, bucket thresholds, mutation vectors, reasons, and rejection reasons are present in `reports/new-formation-pilot/retention.json` and `retention.md`.
- No arena evaluation, worker invocation, active-library mutation, candidate promotion, deployment, or non-pilot output occurred during retention analysis.

## Retained Review Pool

1. `cand_halfrush_1_2fa134`: 92.9% refined, medium novelty, performance baseline.
2. `cand_prayer_6_754069`: 54.8% refined, heavy novelty, prayer coverage.
3. `cand_fullrush_2_1a723c`: 38.1% refined, heavy novelty, fullrush coverage.
4. `cand_halfrush_4_ec12cc`: 33.3% coarse, heavy novelty, distinct direction coverage.

## Residual Risk

The evaluated sample remains small. The retained pool is a review artifact and not approval to apply candidates to `FORMATION_LIBRARY`.
