STATUS: DONE

# T018 Acceptance - Candidate-Level Parallel Tree Optimization Runner

## Decision

Accepted.

## Verified

- The runner schedules independent frozen candidates, not inner tree-search operations.
- Effective capacity is `max(1, min(16, availableLogicalCpus, candidateCount))`.
- On 16 logical CPUs and 24 candidates, the production policy permits at most 16 concurrent candidate workers; remaining candidates queue.
- Each candidate receives the canonical eight-opponent panel and distinct deterministic search/validation seeds.
- Results are restored to input order and individual errors/no-ops are isolated.
- Worker-local optimizer state does not concurrently write shared experience/output state.
- T018 focused scheduling tests report 4/4 pass and protected artifacts remain unchanged.
- No tree algorithm, active formation, deployment, or bundle artifact was modified.
