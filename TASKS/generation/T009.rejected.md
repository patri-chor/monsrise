STATUS: REJECTED

# T009 Rejection - Candidate Retention Used Stale Dry-Run Input

## Decision

Rejected. The implementation and focused tests run, but the delivered retention result does not analyze the current evaluated pilot dataset required by T009.

## Reproduced Evidence

- `npx vite-node tests/candidate_retention.test.ts` passes 8/8.
- The documented CLI succeeds, but its output reports `analyzed: 3`, `retained: 3`, and `rejected: 0`.
- The resulting `retention.md` records every retained candidate with `Performance: 0.0% (source: none)` while still assigning `performance_baseline`.
- T008's evaluated-run diagnostics previously recorded six generated/coarse-evaluated candidates, three refined candidates, and no evaluation failures. The currently overwritten `candidates.jsonl` is an earlier dry-run dataset with no evaluation fields.
- T009 requires the current evaluated dataset and ranking by refined or coarse `adScore`; candidates with no evaluation score cannot satisfy that acceptance path.

## Required Correction

Do not run another arena evaluation: T008 permits exactly one bounded evaluated run. Recover the existing evaluated dataset from a durable, auditable local source if available, isolate test fixtures/output from `reports/new-formation-pilot/`, and rerun retention only after the restored dataset contains evaluated candidates. A follow-up task will define the bounded repair.
