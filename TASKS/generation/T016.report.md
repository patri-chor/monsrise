STATUS: DONE

# T016 - Fixed-Panel Optimizer Consumer API Delivery Report

> Domain: `generation` | Decision owner: generation decision agent | Executor branch: `agent/generation`

## 1. Executive Summary & Deliverables

本任务已按照 T016 规范在 [`src/engine/tree/branch_induct.ts`](file:///d:/develope/monsrise1/src/engine/tree/branch_induct.ts) 中**导出了向后兼容的 `OptimizeFormationOptions` 接口，并实现了调用方完全可控的固定对手面板（Custom Opponent Panel）能力**：

1. **导出类型定义与函数签名向后兼容升级 ([`branch_induct.ts`](file:///d:/develope/monsrise1/src/engine/tree/branch_induct.ts))**：
   ```typescript
   export interface OptimizeFormationOptions {
     opponents?: Formation[];       // 调用方传入的自定义固定评估面板
     searchSeedBase?: number;       // 搜索阶段种子基底
     validationSeedBase?: number;   // 独立验证集种子基底
   }

   export function optimizeFormation(
     BundleAI: any,
     src: Formation,
     gamesPerOpp: number,
     options?: OptimizeFormationOptions,
   ): OptimizeFormationResult | null;
   ```
2. **严格的面板行为实现 (Strict Panel Scope & Behavior)**：
   - **省略 `options` 或 `options.opponents`**：100% 保留现有默认全量 `FORMATION_LIBRARY`（11 阵型）行为；
   - **显式传入 `options.opponents`**：严格以此面板为边界，用于对局轨迹采集、崩盘诊断、样本派生、命中对手过滤、分支搜索以及独立验证集评估；
   - **空面板传入拦截**：若传入 `opponents: []`，在启动任何模拟前直接抛出错误：`OptimizeFormationOptions.opponents cannot be empty`；
   - **严禁隐式回退**：严禁隐式并入或回退到全量 `FORMATION_LIBRARY`。
3. **完全保留核心优化与验证算法 (No Algorithmic Drift)**：
   - 未改动任何分支信息增益（IG）分裂算法、分支变异算子、经验库机制或独立验证集改善判定门槛（`undefeated >= before + 0.05 && loss <= before.loss`）。

---

## 2. Changed Files

- **[MODIFY]** [`src/engine/tree/branch_induct.ts`](file:///d:/develope/monsrise1/src/engine/tree/branch_induct.ts): 导出 `OptimizeFormationOptions` 和 `loadBundle`，支持 `options.opponents` 并向各阶段严格传递。
- **[MODIFY]** [`src/engine/tree/first_four_generation.ts`](file:///d:/develope/monsrise1/src/engine/tree/first_four_generation.ts): 更新接口检测判定为支持状态。
- **[NEW]** [`tests/optimizer_panel_api.test.ts`](file:///d:/develope/monsrise1/tests/optimizer_panel_api.test.ts): T016 专项验收测试套件。
- **[MODIFY]** [`tests/first_four_generation.test.ts`](file:///d:/develope/monsrise1/tests/first_four_generation.test.ts): 适配新接口支持断言。
- **[NEW]** [`TASKS/generation/T016.report.md`](file:///d:/develope/monsrise1/TASKS/generation/T016.report.md): 本交付报告。

---

## 3. Test Verification & Evidence

### 3.1 专项测试结果 (`tests/optimizer_panel_api.test.ts`)
```bash
npx vite-node tests/optimizer_panel_api.test.ts
```
**结果**: **5/5 Pass**
- [Test 1] 验证空面板拦截：`assertStrict.throws(() => optimizeFormation(BundleAI, testSrc, 1, { opponents: [] }))` 成功拦截 (Pass ✓)；
- [Test 2] 默认全量兼容：未提供 options 时完整遍历 11 个默认阵型 (Pass ✓)；
- [Test 3] 自定义 2 阵型面板严格隔离：对局轨迹采集与匹配对手仅限于 `custom2Panel` (Pass ✓)；
- [Test 4] 生成域 8 对手标准面板完整传递：在 8 对手面板上成功进行分支搜索与独立验证集评估，诊断中仅出现 8 对手面板内的阵型 (Pass ✓)；
- [Test 5] 受保护生产文件 byte-identical 零污染 (Pass ✓)。

### 3.2 全量回归测试 (33/33 Pass)
- `tests/optimizer_panel_api.test.ts`: **5/5 Pass**
- `tests/per_seed_expansion.test.ts`: **7/7 Pass**
- `tests/first_four_generation.test.ts`: **5/5 Pass**
- `tests/candidate_retention.test.ts`: **9/9 Pass**
- `tests/new_formation_pilot.test.ts`: **8/8 Pass**

---

## 4. Safety & Scope Confirmation
- [x] **无算法漂移**：未修改任何树优化分支选择算子、搜索动作或经验库逻辑；
- [x] **活跃库零污染**：`FORMATION_LIBRARY` 100% 保持未修改；
- [x] **无部署或构建**：未修改 `public/ai-bundle.iife.js` 或调用 `apply_optimized.ts`；
- [x] **无 24 候选周期运行**：未在 T016 中启动 24 候选优化，完整保留至 T015 消费。
