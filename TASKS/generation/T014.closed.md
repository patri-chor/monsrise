STATUS: DONE

# T014 Acceptance - T013 Worker Concurrency Override

## Decision

Accepted.

## Verified

- `resolveEffectiveWorkers()` defaults to requested worker count `16`.
- Effective concurrency is deterministically clamped to `min(16, available logical CPUs)` with a minimum of one worker.
- The production per-seed manifest records `requestedWorkers: 16`, `effectiveWorkers: 16`, and `availableLogicalCpus: 16`.
- `npx vite-node tests/per_seed_expansion.test.ts` passes 7/7, including default-16, low-CPU clamping, explicit lower test concurrency, per-seed retention, isolation, and active-library protection.
- T013's seed definitions, eight-opponent panel, 20-attempt bound, per-seed maximum-six retention, 25% floor, and isolated output layout are unchanged.
- No tree optimizer, deployment, active-library mutation, or bundle action occurred.

## Note

Focused tests deliberately use fewer workers to limit test cost. Production/default T013 execution now requests 16 workers and is capped only by host CPU availability.

## Follow-up

The 24 frozen candidates remain ready for generation-stage tree optimization once the tree domain exposes an accepted fixed-eight-opponent panel option on the optimizer.
