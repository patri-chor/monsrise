STATUS: DONE

# T013 Acceptance - Per-Seed Variant Expansion and Independent Retention

## Decision

Accepted. T013 establishes the required per-seed retention unit and produces the frozen candidate pool for later generation-stage tree optimization.

## Verified

- Four bundle-order seeds each received 20 bounded attempts.
- The fixed panel contains the bundle first seven formations plus `壕炸金猴`, with both sides evaluated.
- 37 structurally valid generated variants were screened; every seed independently retained six candidates, yielding the intended frozen pool of 24 candidates.
- Per-seed retention no longer uses a global capacity competition. The 25% exploration floor and zero-score exclusion are enforced.
- Cross-seed canonical/team-tree duplicates have deterministic earliest-seed ownership.
- Outputs are isolated under `reports/new-formation-generation/per-seed-expansion/`; historical pilot and first-four-cycle artifacts remain unchanged.
- The T013 report records no tree optimization, deployment, active-library mutation, or bundle modification.

## Verification Note

T013 ran with the former two-worker setting. This does not invalidate its candidate evidence because resource concurrency changes execution speed rather than the fixed deterministic screening contract. T014 is already open to make future generation screening request 16 workers, capped by available logical CPUs. T013 will not be rerun solely for concurrency.

## Follow-up

The 24 frozen candidates are the input to later generation-stage tree optimization once the tree domain publishes an accepted fixed-eight-opponent option for `optimizeFormation`. No candidate is approved for active-library application.
