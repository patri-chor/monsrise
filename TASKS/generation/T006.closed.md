STATUS: DONE

# T006 Acceptance - New Formation Generation Pilot

## Decision

Accepted after corrective delivery recorded in `TASKS/generation/T007.report.md`.

## Verified

- `npx vite-node tests/new_formation_pilot.test.ts` passes 5/5:
  - seed schedules are deterministic and disjoint for coarse `1000` and refined `9000` bases;
  - `prayer`, `halfrush`, and `fullrush` resolve to explicit archetype-appropriate reference formations and missing mappings throw;
  - dry-run generates six isolated, deduplicated candidates across at least three paths;
  - oversized generation returns `ATTEMPT_CAP_EXHAUSTED` at the configured cap;
  - `FORMATION_LIBRARY` is unchanged.
- The pilot reuses the extracted assembly rules from `generate_variants.ts` rather than retaining a diverging generator.
- Candidate records contain generation identity, canonical team/tree identifiers, source metadata, reference formation, tree, structural verdict, and evaluator configuration fields when evaluation is run.
- Coarse and refined evaluations now pass their actual distinct seed bases into `evaluateBatchParallel`.
- Outputs remain restricted to `reports/new-formation-pilot/`; the active library and bundle artifacts are not modified.
- Arena evaluation was not run while T005 remained active. No candidate quality, ranking, or adoption conclusion follows from this acceptance.

## Residual Risk

The accepted evidence is dry-run and focused unit coverage, not real arena quality evidence. A later generation-domain task must run a bounded evaluated pilot only after the tree track is idle and its resource gate is clear.

## Next Decision Gate

T008 will add an operational resource gate and a reproducible bounded evaluated-run workflow. It must remain candidate-only and produce no active-library mutation.
