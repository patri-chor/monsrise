STATUS: DONE

# T012 Acceptance - First-Four Cycle Output Isolation Repair

## Decision

Accepted for its scoped output-isolation repair.

## Verified

- `npx vite-node tests/first_four_generation.test.ts` passes 5/5.
- The focused test writes its generated artifacts only under `tests/.tmp/first-four-generation/`; its output contains no `reports/new-formation-pilot/` write path.
- Independent before/after SHA-256 comparison confirms byte-identical preservation of all eight protected production artifacts:
  - the pilot candidate and retention files;
  - the first-four-cycle manifest, generated candidate, retention, and summary files.
- `npx vite-node tests/candidate_retention.test.ts` passes 9/9.
- `npx vite-node tests/new_formation_pilot.test.ts` passes 8/8.
- `FORMATION_LIBRARY` remains unchanged. No arena evaluation, worker activity, tree optimization, deployment, or apply action occurred.
- The accepted T011 first-four seed screening artifacts remain intact.

## Scope Note

The delivery also changed `scripts/check-open-tasks.mjs`, which is task-system behavior and outside T012's output-isolation scope. It was not evaluated or accepted as part of this decision. Any future task-system behavior change must be issued and reviewed through the dedicated task-generation workflow.

## Remaining Prerequisite

The six retained first-four-cycle candidates are ready for generation-stage tree optimization only after the tree domain supplies a public, backwards-compatible fixed-opponent-panel option for `optimizeFormation`. No candidate has been applied to the active formation library.
