STATUS: DONE

# T017 Acceptance - Sequential Per-Seed Variant Cycle

## Decision

Accepted as the authoritative replacement for T013's batch-order candidate pool.

## Verified

- Seeds run strictly in bundle order: `泉水剑`, `坚果救星`, `全二冲`, `经典救星`.
- Each seed completes generation, deduplication, fixed-eight-panel both-side coarse evaluation, independent retention, and durable freeze before the next seed starts.
- Each source uses at most 20 attempts and retains at most six candidates; the delivered cycle froze 24 candidates with no shortfall.
- Production requested and recorded 16 workers on a 16-logical-CPU host; effective workers were 16.
- T017 focused tests report 3/3 pass and the protected production/history artifacts remain unchanged.
- The authoritative input is `reports/new-formation-generation/sequential-per-seed-cycle/frozen_candidates.jsonl`; T013's batch pool is historical only.
- No tree optimization, deployment, active-library mutation, or bundle mutation occurred.
