STATUS: PARTIAL

# T015 - Frozen Candidate Fixed-Panel Tree Optimization Delivery Report

> Domain: `generation` | Decision owner: generation decision agent | Executor branch: `agent/generation`

## 1. Executive Summary & Hard Prerequisite Gate

本任务按照 T015 规范，在启动 24 个冻结候选阵型的决策树优化流水线前，严格执行了**公共树优化器接口硬前置条件（Hard Prerequisite）审计**：

1. **输入与固定 8 对手面板确认 (Input & Fixed Panel Verified)**：
   - 消费输入：[`reports/new-formation-generation/per-seed-expansion/frozen_candidates.jsonl`](file:///d:/develope/monsrise1/reports/new-formation-generation/per-seed-expansion/frozen_candidates.jsonl)（24 个满额冻结候选）；
   - 固定 8 对手面板：`泉水剑`, `坚果救星`, `全二冲`, `经典救星`, `全二永平`, `肃清`, `梯子塞雷`, `壕炸金猴`（8 个唯一对手互斥完整）。
2. **硬前置条件缺失诊断与主动阻断 (Hard Prerequisite Gate - STATUS: PARTIAL)**：
   - 依据 T015 规范强制约束：“*The supplied panel must be used for trace collection, branch induction, matched-opponent filtering, branch search, and independent validation. If this interface is not present and tested on the current branch, write TASKS/generation/T015.report.md with STATUS: PARTIAL, identify the missing tree-domain prerequisite, and stop without running optimizer, arena, or generation work. Do not change tree optimizer source files in this task.*”
   - 审计现有公共树优化入口 [`src/engine/tree/branch_induct.ts:optimizeFormation`](file:///d:/develope/monsrise1/src/engine/tree/branch_induct.ts)：
     - 其当前签名仅为 `(BundleAI, src, gamesPerOpp, options?: { searchSeedBase?: number; validationSeedBase?: number })`；
     - 内部在轨迹采集（Line 491）、样本衍生及独立验证（Line 644）时**硬编码遍历全量 `FORMATION_LIBRARY`（11 个阵型）**，无法接收调用方指定的 `options.opponents?: Formation[]` 限制在固定 8 对手面板；
   - 严格遵循双域 Git 总线跨域契约，未在 generation 域擅自修改 tree 域代码，主动停止执行树优化和对战评估，按规范出具 `STATUS: PARTIAL` 报告。

---

## 2. Missing Tree-Domain Prerequisite Specification

为了使生成域能够无缝调用树优化器在固定面板上优化新卡组候选，请求 tree 域或决策方在 `tree` 域分支实现并交付如下接口规范：

### 2.1 扩展的公共函数签名
```typescript
export interface OptimizeFormationOptions {
  opponents?: Formation[];       // 允许调用方传入自定义固定评估面板（如 8 对手面板）
  searchSeedBase?: number;       // 搜索阶段种子基底
  validationSeedBase?: number;   // 验证阶段种子基底
  workerCount?: number;          // 并发评估 worker 数
}

export function optimizeFormation(
  BundleAI: any,
  src: Formation,
  gamesPerOpp: number,
  options?: OptimizeFormationOptions,
): OptimizeFormationResult | null;
```

### 2.2 内部面板行为要求
- 初始对局轨迹采集（Trace Collection）：使用 `options.opponents ?? FORMATION_LIBRARY`；
- 崩盘诊断与分裂样本派生：基于传入面板产生的轨迹；
- 独立验证集评估（Independent Validation）：使用 `options.opponents ?? FORMATION_LIBRARY`。

---

## 3. Test Verification & Dataset Integrity

### 3.1 前置审计测试全绿 (4/4 Pass)
```bash
npx vite-node tests/frozen_tree_optimization.test.ts
```
**结果**: **4/4 Pass**
- [Test 1] 验证 T013 冻结 24 候选数据集可用性 (24 条有效记录)；
- [Test 2] 验证固定 8 对手面板规范解析 (8 唯一对手)；
- [Test 3] 准确判定现有优化器接口缺少 `options.opponents` 支持；
- [Test 4] 验证所有 14 个受保护生产文件及 `FORMATION_LIBRARY` 均为 100% byte-identical。

### 3.2 统计指标 (Processed / Gate Blocked)
- **Input Candidates in Pool**: 24
- **Processed Candidates**: 0 (Blocked by Missing Interface Gate)
- **Quality Gate Decision**: `BLOCKED_WAITING_TREE_PREREQUISITE`

---

## 4. Safety Confirmation
- [x] **无优化器越界调用**：检测到接口缺失后主动安全停止；
- [x] **无 tree 域文件修改**：未修改 `branch_induct.ts`、`tree_ops.ts` 等 tree 域源码；
- [x] **活跃库零污染**：`FORMATION_LIBRARY` 100% 保持未修改；
- [x] **历史产物零破坏**：`reports/new-formation-pilot/`、`reports/new-formation-generation/first-four-cycle/` 与 `reports/new-formation-generation/per-seed-expansion/` 所有历史产物 100% byte-identical 保留。
