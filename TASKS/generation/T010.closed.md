STATUS: DONE

# T010 Acceptance - Restore Evaluated Pilot Dataset and Re-run Retention

## Decision

Accepted.

## Verified

- T010 restored the six-candidate evaluated T008 dataset without an additional arena run.
- The restored dataset contains six coarse evaluations and three refined evaluations using the accepted fixed settings: seed `42`, two workers, coarse `2` games/base `1000`, refined `6` games/base `9000`.
- `npx vite-node tests/candidate_retention.test.ts` passes 9/9.
- `npx vite-node tests/new_formation_pilot.test.ts` passes 8/8.
- A fresh production retention CLI run preserves `reports/new-formation-pilot/candidates.jsonl` byte-for-byte (SHA-256 identical before and after).
- Test outputs are isolated beneath `tests/.tmp/`; production pilot data is no longer overwritten by tests.
- No worker, arena, deployment, active-library, or non-pilot output mutation occurred.

## Follow-up

T009 is now accepted using the restored evaluated input. No next evaluation or promotion task is opened automatically; candidate adoption requires an explicit user decision and a separately scoped integration task.
