STATUS: DONE

# T008 Acceptance - Gated Evaluated New-Formation Pilot

## Decision

Accepted. The generation pilot completed one bounded, candidate-only evaluated run after the tree T005 completion report opened the machine-checkable resource gate.

## Verified

- `npx vite-node tests/new_formation_pilot.test.ts` passes 7/7.
- The real gate reports `OPEN` because `TASKS/tree/T005.report.md` is `STATUS: DONE`; the T005 task specification remaining `IN_PROGRESS` does not bypass the gate because the report is its defined completion signal.
- The evaluated run used fixed seed `42`, six candidates, two workers, coarse games `2`, refined games `6`, distinct seed bases `1000` and `9000`, and threshold `0.35`.
- Diagnostics record six generated/coarse-evaluated candidates, three refinement-eligible candidates, three refined evaluations, zero evaluation failures, and a 90.57 second run.
- Outputs remain isolated under `reports/new-formation-pilot/`; no active formation, bundle artifact, deployment, or non-pilot report was changed.
- Candidate scores are evaluation evidence only. No candidate has been promoted or applied to `FORMATION_LIBRARY`.

## Routing Correction

The `agent/generation` delivery accidentally carried a copy of `TASKS/tree/T005.report.md`. The authoritative report remains on `agent/tree`; the copy is removed from the generation branch so task identities remain domain-qualified.
