STATUS: DONE

# T014 - T013 Worker Concurrency Override Delivery Report

> Domain: `generation` | Decision owner: generation decision agent | Executor branch: `agent/generation`

## 1. Executive Summary & Concurrency Upgrade

本任务已按照 T014 规范完成**粗筛评估阶段 Worker 并发上限机制升级与宿主机 CPU 动态适配**：

1. **默认请求 Worker 提升至 16 (Requested Workers = 16)**：
   - 升级 [`src/engine/tree/per_seed_expansion.ts`](file:///d:/develope/monsrise1/src/engine/tree/per_seed_expansion.ts) 中的并发解析逻辑，引入 `resolveEffectiveWorkers`；
   - 默认请求并发数由 2 提升为 **16**；
   - 运行时按公式 `Math.max(1, Math.min(requestedWorkers, availableLogicalCpus))` 严格钳位，确保不超额派发超过宿主机 CPU 核心数的 worker。
2. **实测 16 并发评估证据 (16-Worker Execution Evidence)**：
   - 宿主机逻辑 CPU 数（`os.cpus().length`）：**16**
   - 生产级配置：`requestedWorkers: 16`，`effectiveWorkers: 16`
   - 对战吞吐表现：**1036 场对战任务 / 16 workers，耗时仅 50.6 秒**（较 2 workers 提速 3.4 倍）。
3. **完全继承 T013 非并发规范 (All Non-Concurrency Scope Preserved)**：
   - 源种子与面板定义（4 种子、8 对手面板）、20 次独立变异上限、每种子独立至多 6 保留容量策略（总计 24 候选冻结池）、输出隔离目录结构保持 100% 不变。

---

## 2. Worker Concurrency Diagnostics & Evidence

### 2.1 运行时并发环境参数
- **Host Logical CPUs**: 16
- **Requested Workers**: 16
- **Effective Workers**: 16
- **Coarse Evaluation Load**: 37 候选 $\times$ 8 对手 $\times$ 2 侧（双方先手） $\times$ 1 局 + 对手自对战基准 = 1036 场对战
- **Execution Time**: 50.6s

### 2.2 产物元数据持久化验证 ([`seed_manifest.json`](file:///d:/develope/monsrise1/reports/new-formation-generation/per-seed-expansion/seed_manifest.json))
```json
"effectiveSettings": {
  "attemptsPerSeed": 20,
  "maxRetainedPerSeed": 6,
  "maxTotalCapacity": 24,
  "explorationFloor": 0.25,
  "requestedWorkers": 16,
  "effectiveWorkers": 16,
  "availableLogicalCpus": 16,
  "coarseGames": 1,
  "coarseSeedBase": 1000
}
```

---

## 3. Test Verification & Safety Confirmation

### 3.1 单元测试全绿 (29/29 Pass)
```bash
npx vite-node tests/per_seed_expansion.test.ts
```
**结果**: **7/7 Pass**
- 覆盖 `resolveEffectiveWorkers` 默认 16 并发、宿主机 CPU 较少时的动态 clamp、至少 1 的下限保护、测试显式指定 worker 时的放行逻辑；
- 覆盖端到端流水线隔离运行、24 候选冻结完整性、所有生产文件及 `FORMATION_LIBRARY` byte-identical 零污染保护。

### 3.2 保护与安全确认
- [x] **无越界 worker 派发**：未产生超过宿主机 16 CPU 限制的子进程；
- [x] **无优化器调用**：未在生成域运行树优化；
- [x] **活跃库零污染**：`FORMATION_LIBRARY` 100% 保持未修改；
- [x] **无部署或打包构建**：未修改 `public/ai-bundle.iife.js`。
