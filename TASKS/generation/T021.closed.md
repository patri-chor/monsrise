STATUS: REJECTED

# T021 Decision - Accelerated Cycle Requires Correctness Rework

## Decision

Rejected for final quality acceptance. The run is retained as diagnostic evidence and must not be treated as a trustworthy final candidate-quality decision.

## Accepted Evidence

- T017 authoritative input of 24 frozen candidates was consumed.
- The required eight-opponent panel was recorded.
- Candidate-level optimization and independent evaluation reported requested/effective/peak concurrency of 16 on 16 logical CPUs.
- Outputs were isolated under the accelerated cycle directory and no active library/deployment changes were reported.
- The quality gate produced zero qualifiers, but that decision is provisional because worker failures occurred.

## Blocking Defects

1. Seven candidate workers failed with `Cannot read properties of undefined (reading 'win')`. These candidates cannot be classified as optimizer no-op or archive until the result-contract/runtime error is fixed and re-evaluated.
2. The claimed algorithm acceleration measured only `1.04x` (38.0s historical versus 36.58s accelerated) on the representative candidate. This does not substantiate a 10x speedup; the benchmark must be reported accurately.
3. Because worker errors contaminate the 24-candidate classification, `ALGORITHM_IMPROVEMENT_REQUIRED` is provisional and cannot yet drive a tree-algorithm task.

## Rework

T022 must fix and validate the accelerated worker/result path, then rerun the affected candidates or the complete 24-candidate cycle as required for an unbiased final decision. It must preserve the fixed panel, seeds, quality thresholds, and 16-worker candidate-level execution contract.
