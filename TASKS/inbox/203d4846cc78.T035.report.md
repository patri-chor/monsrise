STATUS: DONE
DOMAIN: tree

# T035 Instrumented Product-Path Rerun

Protocol: `PRODUCT_PATH_FORMAL_SCREEN_T035_V1`  
Manifest hash: `7bb394b394eba26466ec6d7ee4ed3489cd2b8fc966edda97c81452f103c13d61`

## Result

The fixed inventory was rerun through `PersistentSimPool` product tasks only (`playFullGame` + declarative tree
strategy). No arena formal task, mutation generation, H2H, promotion, Tier change, apply, deploy, or active bundle
change occurred. T032 aggregate and historic sandbox files remain untouched.

- Worker concurrency: configured/observed in `manifest.json`; pool retained.
- Inventory: 10 executable baselines; 60 existing candidates; 7 held-out families x 2 sides x 10 games.
- Raw cells: 9,800 total (`70 x 140`), each complete with zero worker error, nonempty teams, both-side accepted
  placement evidence, source side, exact deterministic seed, W/D/L, round scores, branch/product trace link.
- Four-cost ledger: 64 individual trace-backed product records, all `PASS`; no `MISSING_TRACE`.
- Recompute audit: all 10 baselines and 60 candidates have exactly 140 cells; W/D/L and training score recompute
  exactly from raw cells; both sides and all seven opponents are present.

## Evidence

All append-only evidence is under `tests/fixtures/tree/experience_library/product_path_t035/`:

```text
manifest.json
candidate_registry.jsonl
source_baselines.jsonl
candidate_cells.jsonl
candidate_observations.jsonl
four_cost_fidelity_ledger.jsonl
traces.jsonl
cursor.json
frontiers.json
README.md
```

The read-only verification command was:

```text
npx vite-node scripts/audit_t035_product_path.ts
```

It passed with `registry=60`, `baselines=10`, `candidates=60`, `cells=9800`, `traces=9800`, `fourCost=64`.

## Duplicate / No-op Finding

Canonical topology/team fingerprints identified 10 duplicate groups. They remain in the registry with their separate
candidate IDs and are not treated as independent evidence. Notable groups include the three-way groups for
`springsword`, `nutsavior`, `suqing`, `spade_multi`, and `golden_boom`; all duplicate identities are recorded in
`candidate_registry.jsonl`.

## Frontier Signal Only

The recomputed frontier file retains the product path signal only, `tierPromotion=NOT_PERFORMED`. Five frontiers score
140/0/0: `cand_s0_1_light_spri`, `cand_s1_1_light_nuts`, `cand_s5_1_light_suqi`,
`cand_s8_2_light_gift`, and `cand_s9_3_medium_gold`. Unlike T032, each has all 140 raw cells with nonempty candidate
and opponent teams, valid side mapping, accepted placements on both sides, trace-linked round outcomes, and no early
or protocol termination. They pass T035 integrity checks but remain `PRODUCT_PATH_SCREEN_SIGNAL_ONLY`, `NOT_TIER_2`.

## No-Apply Confirmation

No candidate was promoted, applied, deployed, or used to alter an active formation/bundle. `gift_jungle` remains the
frozen seven-monster legacy record.
