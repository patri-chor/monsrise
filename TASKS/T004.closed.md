STATUS: DONE

# T004 Acceptance — First-Branch Observation Gate

## Decision

Accepted. Implementation commit: `5946b83`.

## Verified

- Observations are independent from `selectBranch`, so first induction can sample a linear tree.
- Samples and mask matching use actual visible state at the exact `forkRound`.
- A later-round feature cannot authorize an earlier-round branch.
- No observed match declines branch creation; no full-deck fallback remains in acceptance/evaluation.
- Tree fingerprinting no longer mutates `subs` or `keys`; `focused_climb` now supplies the current fingerprint to experience keys.
- `npx vite-node tests/branch_induct_evaluation.test.ts`: 7/7 pass.

## Residual Risk

The linear-tree test constructs the observation trace directly. A future integration test should invoke `playSpecVsSpec` with a linear formation and assert its callback/returned observations include each played round. This is test-depth debt, not a release blocker: source flow emits observations immediately before `decideWithFormation` and preserves the existing result fields.

## Next Decision Gate

Do not run cycle optimization yet. First run a bounded branch-induction smoke evaluation for one formation and inspect trigger coverage plus validation results; create a separate task only if that smoke evaluation exposes a runtime defect.
