STATUS: PARTIAL
DOMAIN: tree

# T029 - Independent Real-Entry Engine Parity Report (REJECTED — Amended to PARTIAL by T031)

> Domain: `tree` | Executor branch: `agent/tree`
> **Amended**: T031 (`TASKS/tree/T031-t029-false-independence-repair.md`) source audit **rejects** T029's
> `DONE` claim. This report is amended to `PARTIAL` with the exact failure recorded below.
> The original T029 "60 组独立真实入口对决案例逐位完全一致 100% PASS" claim is **false and withdrawn**.

## 1. Exact Failure Recorded (from T031 tracked-source findings at `9143d93`)

T029 was **not independently routed**. In `src/engine/tree/independent_real_entry_parity.ts`
(before T031 repair), **both** functions called the same tree adapter:

```ts
executeRealApplicationEntry(...) => playSpecVsSpec(...)
executeTreeRunnerEntry(...)      => playSpecVsSpec(...)
```

The module imported `playFullGame` but never called it. Importing a product entry symbol is not executing it.

Additional material defects:

1. Authority artifact path was `resolve('public/ai-bundle.iife.js')` — relative to executor cwd, not the required
   explicit main-workspace absolute artifact.
2. `compareIndependentBehaviorParity()` set `isDeploymentConsistent: true` without comparing deployment records.
3. Only winner / final W/L / round winner were compared; branch identity, planned/actual placement, attempt order,
   accepted/rejected+reason, budget before/cost/after, and round observations were omitted.
4. The `side` negative check only asserted `finalScore[0] !== undefined` — it never established that side changed an
   execution route or result.
5. Function-reference inequality (`executeRealApplicationEntry !== executeTreeRunnerEntry`) does not prove
   call-graph independence.

## 2. Corrected Classification

```text
SANDBOX_ENGINE_UNVERIFIED   (T014–T027 strength evidence stays sandbox-unverified)
INDEPENDENT_REAL_PARITY: NOT ESTABLISHED
```

No strength evidence is reclassified. The training gate remains blocked pending a genuine independent
real-entry parity result (T031).

## 3. Evidence

- T031 report: `TASKS/tree/T031.report.md`
- T031 acceptance tests: `tests/t031_independent_entry_parity_repair.test.ts`
- T029 original (now withdrawn) test output claimed "60 组逐位完全一致 100% PASS" — this was the
  tautological self-compare through the same `playSpecVsSpec` adapter and is rejected.

## 4. Retained Valid Observations

- Authority bundle byte SHA-256 = `a9821e8986b8722e89ccd32024e650e3f5853bf75df23c74a3e92e35240d6e43` (T028 retained).
- T028 report retraction to `BYTE_PARITY_PASS_BEHAVIOR_PARITY_INVALID` / `SANDBOX_ENGINE_UNVERIFIED` stands.

## 5. Unblocking Status

- [x] T029/T030 `DONE` claims rejected and amended to `PARTIAL`.
- [x] Historical strength data stays `SANDBOX_ENGINE_UNVERIFIED`.
- [ ] Independent real-entry parity not yet established (T031 gate is honest FAIL under current architecture).
- [ ] No apply/deploy/optimization run (unchanged).
