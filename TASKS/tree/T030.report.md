STATUS: PARTIAL
DOMAIN: tree

# T030 - T029 Tracked Delivery and Provenance Audit Report (REJECTED — Amended to PARTIAL by T031)

> **Amended**: T031 source audit rejects the T029 implementation it was auditing. T030's conclusion that the
> T029 implementation is "已交付 / 可视为已交付" is **withdrawn**; this report is amended to `PARTIAL`.

## Exact Failure Recorded

T030's audit verified that T029's files were tracked at `9143d93`, but did not catch the false-independence defect:
both `executeRealApplicationEntry` and `executeTreeRunnerEntry` call the same tree adapter `playSpecVsSpec`,
and `playFullGame` was imported but never invoked. Tracked delivery ≠ correctness.

The T029 implementation is **not independently routed** and its behavioral parity PASS is **invalid**:

```ts
executeRealApplicationEntry(...) => playSpecVsSpec(...)
executeTreeRunnerEntry(...)      => playSpecVsSpec(...)
```

Therefore T030's `DONE` claim is rejected. See `TASKS/tree/T031-t029-false-independence-repair.md` and
`TASKS/tree/T031.report.md` for the full defect record and repair.

## Corrected Classification

```text
T030: PARTIAL   (audited artifact existed in tree but its content was a false-independent tautology)
T029: PARTIAL   (DONE claim rejected)
SANDBOX_ENGINE_UNVERIFIED  (T014–T027 strength evidence unchanged)
```

## Files Now Considered Authoritative

- `TASKS/tree/T031.report.md` — independent real-entry parity repair report.
- `src/engine/real_application_entry.ts`, `src/engine/canonical_trace.ts`, `src/engine/tree/independent_real_entry_parity.ts`
  — T031 repaired implementation.
- `tests/t031_independent_entry_parity_repair.test.ts` — T031 acceptance tests.
