# Task Report: T135 - Flatten Tree Runtime and Archive Old

- **Task**: `TASKS/tree/T135-flatten-tree-runtime-and-archive-old.md`
- **Status**: `DONE`
- **Domain**: `tree`
- **Branch**: `agent/tree`
- **Author**: Antigravity

---

## 1. Summary of Changes

T135 completed the complete architectural consolidation and flattening of the decision tree optimization system:

1. **Shared Battle Infrastructure (`src/engine/`)**:
   - [`src/engine/round_board.ts`](file:///d:/develope/monsrise1/src/engine/round_board.ts): Pure data definitions for single-round board state (`RoundBoardState`) and edit deltas (`RoundBoardEdit`).
   - [`src/engine/round_board_factory.ts`](file:///d:/develope/monsrise1/src/engine/round_board_factory.ts): State capture from full-game baseline replays, state fingerprint calculation, and edit cloning.
   - [`src/engine/single_round_engine.ts`](file:///d:/develope/monsrise1/src/engine/single_round_engine.ts): Single-round headless real engine battle runner.
   - [`src/engine/product_match_runner.ts`](file:///d:/develope/monsrise1/src/engine/product_match_runner.ts): Full match execution wrapper and observable round summaries.

2. **Flat Formal Tree Runtime (`src/engine/tree/`)**:
   Consolidated into a minimal, flat module set directly under `src/engine/tree/`:
   - `tree_types.ts`: Formal config, reports, and execution types.
   - `tree_snapshot.ts`: Dynamic and static snapshot resolver.
   - `tree_product.ts`: Strategy adapter and observation wrappers.
   - `tree_benchmark.ts`: Baseline evaluations and adverse case mining.
   - `tree_search.ts`: Local S search and Pareto dominance selection.
   - `tree_deck.ts`: D candidate catalogs and D+S state reconstruction.
   - `tree_lineage.ts`: S/D+S lineage compilation and backpropagation validation.
   - `tree_worker.ts`: Single worker thread payload execution.
   - `tree_worker_pool.ts`: Multi-threaded worker pool with fail-closed deterministic sorting.
   - `tree_dynamic_pool.ts`: Dynamic T0 pool candidate discovery, sorting, and replacement.
   - `tree_evidence.ts`: Structured JSON/JSONL evidence output.
   - `tree_cycle.ts`: Main cycle orchestration engine.

3. **Archive & CLI Consolidation**:
   - Archived legacy experimental files to `src/engine/tree/old/` with a comprehensive [`src/engine/tree/old/README.md`](file:///d:/develope/monsrise1/src/engine/tree/old/README.md).
   - Created single unified CLI entrypoint: [`scripts/run_tree_cycle.ts`](file:///d:/develope/monsrise1/scripts/run_tree_cycle.ts).

---

## 2. Test Verification

All three target test suites are dedicated, passing 100%, and verified:

1. **`tests/tree_cycle_product.test.ts`**:
   - PASS (17.5s).
   - Validates baseline state capture, D+S state reconstruction with accurate D-specific state, Score70 thresholding, lineage compilation, and artifact JSON/JSONL schema.

2. **`tests/tree_cycle_worker.test.ts`**:
   - PASS (31.4s).
   - Validates worker thread task dispatch for S search, D+S, and backprop validation.
   - Validates exact logical parity and 100% deterministic output matching between `parallelBackend: 'single'` and `parallelBackend: 'worker_threads'`.

3. **`tests/tree_cycle_pilot.test.ts`**:
   - PASS (0.1s).
   - Validates dynamic candidate discovery, deterministic selection of top-3 entries, and persistent pool replacement.

4. **CLI Smoke Run**:
   - `npx vite-node scripts/run_tree_cycle.ts --smoke` ran successfully and exited code 0.

---

## 3. Bug Fixes & Quality Improvements

- **BattleSystem Memory Leak**: Cleared map singletons (`screenPositions`, `_targetPositions`, `_attackTimers`) on `startBattle` to prevent heap exhaustion during large-scale simulation sweeps.
- **Worker Seed Offset Alignment**: Added `caseIndexOffset` to guarantee identical mulberry32 RNG streams regardless of parallel worker chunking.
- **Board Coordinate Perspective**: Fixed side 1 placement transform (`10 - rawX`) so compiled tree actions deploy to correct friendly board regions (`x: 0..4`).
