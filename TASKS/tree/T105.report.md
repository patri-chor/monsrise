# T105 任务报告 (T105 Report)

**STATUS: DONE**  
**DOMAIN: tree**  
**TASK ID: T105**  
**SUPERSEDES: T104**  
**BRANCH: agent/tree**  
**COMPLETION TIME: 2026-08-21T00:13:00+08:00**

---

## 1. 任务目标与执行概要 (Mission & Execution Overview)

本任务（T105）完成了 Generation 2 关键验证、真实 Worker 任务归因确定性验证与首个全链路可审计的 All2Rush Pilot 运行：
1. **真实活跃阵型 Manifest 锚定与全链路指纹链约束**：
   - 动态发现活跃全二冲 (`all2rush`) 与选定对手 (`golden_boom`, `all2prayer`) 的确切快照（生成清单 `reports/tree-cycle/all2rush_g2_t105_pilot_manifest.json`）；
   - 校验并持久化完整指纹链：`expectedFingerprint === resolverFingerprint === preparedEvolFingerprint === task target/opponent fp === result target/opponent fp`；
   - 遇未解析/指纹不匹配时写入 append-only quarantine (`reports/tree-cycle/all2rush_g2_t105_quarantine.jsonl`) 并 fail-closed。
2. **真实 Worker 任务归因与全字段确定性验证 (`Worker Task Attribution & Determinism`)**：
   - 扩展 worker 协议，返回 `workerId` (`worker_tid_x`)、`taskId`、双侧 payload/policy 指纹、W/D/L、`roundResults`、布阵 trace 摘要 (`traceDigest`)；
   - 严格通过定向亲和力与多实例运行：
     - 同一任务同 Worker 重复 10 次：100% 相同结果与 traceDigest；
     - 跨两个确认不同的 Worker（Worker 0 与 Worker 1）：100% 相同结果与 traceDigest；
     - 线程池销毁重建后执行：100% 相同结果与 traceDigest；
   - 持久化对比记录至 `reports/tree-cycle/all2rush_g2_t105_worker_determinism.jsonl`。
3. **目标侧 (Target-Side) 严格对齐与真实 Exact Branch 运行时验证**：
   - 严格按 `lossCase.side` 调用 `buildRoundContext(targetSide)`，杜绝侧混淆；
   - 验证目标侧策略命中存储的 exact branch ID（`chosenBranchId` 与存储 ID 完全匹配）；
   - 断点续玩 100% 重现记录的 W/D/L 与 trace 摘要。
4. **真实相似观察 (Similar Observation) 与 Warm-Start 明确隔离**：
   - 构造真实的非完全相同合法雾战观察（修改对手关键单位 ID），验证窄分支不被误触发（`chosenBranch !== storedBranchId`）；
   - 验证该分支动作增量仅作为优化器本地探索的 Warm-Start 初始变体输入，未在运行时直接生效。
5. **No-Apply 守则**：
   - 严格保持全局主树、梯队、L1 权重与现网部署零改动。

---

## 2. 自动化验证结果 (Verification Results)

所有核心测试套件 100% 绿灯通过：

| 测试套件 (Test Suite) | 测试内容 (Scope) | 结果 (Verdict) |
|---|---|---|
| `tests/t105_auditable_active_pilot.test.ts` | 活跃阵型锚定清单、Worker 归因及 10 次/跨 Worker/重建确定性验证、目标侧分支精准命中及真实相似观察隔离、安全合并剪枝 | **100% PASS** |
| `tests/t104_active_snapshot_determinism_and_pilot.test.ts` | 16 局双侧保真矩阵、Worker 多次/跨 Worker/销毁重建确定性、Exact 分支与 Warm-Start 隔离 | **100% PASS** |
| `tests/t103_verified_round_state_and_runtime.test.ts` | 权威状态检查点、精确快照 LossCase 挖掘、动态特征掩码推导与非空合并规则 | **100% PASS** |
| `tests/t102_executable_branches.test.ts` | 验证 Loss Case 挖掘、断点续玩、分支 AST 编译与策略执行 | **100% PASS** |
| `tests/t101_round_engine_fidelity.test.ts` | 16 局保真比对与断点恢复重放 | **100% PASS** |
| `tests/t053_candidate_identity_and_quarantine.test.ts` | 历史快照隔离与身份门禁兼容性回归 | **100% PASS** |

---

## 3. 证据文件清单 (Evidence Artifacts)

生成以下 T105 专属只读证据文件：
1. `reports/tree-cycle/all2rush_g2_t105_pilot_manifest.json`
2. `reports/tree-cycle/all2rush_g2_t105_worker_determinism.jsonl`
3. `reports/tree-cycle/all2rush_g2_t105_loss_cases.jsonl`
4. `reports/tree-cycle/all2rush_g2_t105_local_trials.jsonl`
5. `reports/tree-cycle/all2rush_g2_t105_branch_runtime.jsonl`
6. `reports/tree-cycle/all2rush_g2_t105_merge_prune.jsonl`
7. `reports/tree-cycle/all2rush_g2_t105_quarantine.jsonl`

---

## 4. 变更文件清单 (Changed Files)

- [`src/engine/tree/fine_grained_worker.ts`](file:///d:/develope/monsrise1/src/engine/tree/fine_grained_worker.ts): 扩展 Worker 任务与结果归因协议；
- [`src/engine/tree/persistent_pool.ts`](file:///d:/develope/monsrise1/src/engine/tree/persistent_pool.ts): 支持 Worker 亲和定向分派选项；
- [`src/engine/tree/round_engine/loss_case_inventory.ts`](file:///d:/develope/monsrise1/src/engine/tree/round_engine/loss_case_inventory.ts): 增加快照解析失败 Quarantine 审计；
- [`src/engine/tree/round_engine/branch_first_optimizer.ts`](file:///d:/develope/monsrise1/src/engine/tree/round_engine/branch_first_optimizer.ts): 严格绑定 targetSide 执行续玩搜索；
- [`tests/t105_auditable_active_pilot.test.ts`](file:///d:/develope/monsrise1/tests/t105_auditable_active_pilot.test.ts): T105 专属测试套件；
- [`TASKS/tree/T105-generation2-auditable-active-pilot-execution.md`](file:///d:/develope/monsrise1/TASKS/tree/T105-generation2-auditable-active-pilot-execution.md): 标记状态为 DONE。
