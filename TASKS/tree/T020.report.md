STATUS: DONE
DOMAIN: tree

# T020 - Runtime Evaluation Integrity and Elite-Seed Continuity Report

> Domain: `tree` | Executor branch: `agent/tree`
> Elite Seeds Fixture: `tests/fixtures/tree/persistent_elite_seeds.json`
> Diagnostic Target: Error-Preserving Evaluation Contract, Preflight Gate & Elite Continuity

---

## 1. Executive Summary

已圆满完成 **T020（Runtime Evaluation Integrity and Elite-Seed Continuity）** 专项重构与审计修复：
1. **错误保留评测契约（Error-Preserving Evaluation Contract）**：
   - 彻底修复了 `arena.ts` 和 `fine_grained_worker.ts` 中静默捕获吞异常的缺陷，所有 Worker 异常直接通过 `MatchMetrics` 中的 `workerErrorCount`、`workerErrors` 和 `isEvaluationComplete` 显式向上抛出并持久化记录；
   - 彻底杜绝了将 Worker 异常伪装成 `0W/0D/NL` 普通负场的行为；
2. **训练前引擎级预检门禁（Engine-Level Runtime Preflight）**：
   - 在进入 3 次独立优化尝试前，对候选执行 1 轮（双方视角 $\times$ 7 早期家族）的确定性运行时预检；
   - 任何发生 Worker 异常的候选直接标记为 `RUNTIME_INVALID` 并淘汰，绝不进入 Attempt 聚合或分层库；
3. **建立持久化精英种子池（Persistent Elite-Seed Pool）**：
   - 将 T014 中已验证的 3 套精英候选（`cand_s1_1_2a` 泉水剑、`cand_s1_2_2b` 泉水剑、`cand_s2_1_8e` 坚果救星）完整建立为持久化档案 [`tests/fixtures/tree/persistent_elite_seeds.json`](file:///d:/develope/monsrise-tree-decision/tests/fixtures/tree/persistent_elite_seeds.json)；
   - 在修复后的评估环境中完成了独立真实 Retest，保留其独立历史溯源与比较基准；
4. **全量测试 100% PASS**：新增专项测试 `tests/t020_runtime_integrity_and_elite_seeds.test.ts` 及全部历史回归测试全项通过。

---

## 2. Root-Cause Error Diagnostic & API Repair Table

| Module / Component | Previous Broken Behavior | Repaired T020 Behavior | Diagnostic & Audit Guarantee |
|---|---|---|---|
| `arena.ts:bundleRoundPlanFor` | `catch (e) { return []; }` 静默吞掉注入/Placement 异常 | `catch (e) { throw e; }` 显式向外抛出真实异常堆栈 | 杜绝假空计划与静默空出兵导致的虚假负场 |
| `fine_grained_worker.ts` | 异常转为 `{ w: 0, d: 0, l: task.games }` 伪装普通负场 | 返回 `{ w: 0, d: 0, l: 0, error: msg }` 显式携带错误 | 区分战术打不过（合法负场）与运行时异常（`RUNTIME_INVALID`） |
| `persistent_pool.ts` | 聚合时仅累加 W/D/L，丢弃 Worker 报错 | 在 `MatchMetrics` 中累加 `workerErrorCount`，`isEvaluationComplete = false` | 任何 Worker 异常在顶层直接可见并阻断入库 |
| `eleven_library_training.ts` | 无运行时预检直接进入 90 次优化尝试 | 先执行 1-game Preflight，异常候选直接记入 `runtime_diagnostic_ledger.jsonl` | 淘汰候选原因透明，防止无效计算 |

---

## 3. Persistent Elite Seeds Retest & Continuity Record (`tests/fixtures/tree/persistent_elite_seeds.json`)

| Candidate ID | Source Seed | Provenance Task | Historical T014 Metrics | T020 Retest Status (0 Errors) | Adopted Move Description |
|---|---|---|---|---|---|
| `cand_s1_1_2a` | 泉水剑 | T014 | Held-Out $\Delta = +5.7\%$, Strong $\Delta = +11.9\%$ | **PASS (0 Worker Errors)** | `[常规站位 R1] 可控怪兽 105 → (8,0)` |
| `cand_s1_2_2b` | 泉水剑 | T014 | Held-Out $\Delta = +17.1\%$, Strong $\Delta = +19.4\%$ | **PASS (0 Worker Errors)** | `[常规站位 R1] 可控怪兽 105 → (8,1)` |
| `cand_s2_1_8e` | 坚果救星 | T014 | Held-Out $\Delta = +9.3\%$, Strong $\Delta = +12.5\%$ | **PASS (0 Worker Errors)** | `[入场提前] 怪兽 110 从 R3 提前至 R2` |

---

## 4. Verification Commands & Results

1. **T020 运行时完整性与精英种子连续性专项测试**:
   ```bash
   npx tsx tests/t020_runtime_integrity_and_elite_seeds.test.ts
   ```
   - 结果：**100% PASS**（Worker 异常完整传播与错误保留契约验证通过、3 个 T014 精英种子完整存在并保留历史溯源、独立真实评测 0 Errors 通过）。
2. **T019 Git 追踪归档专项测试**:
   ```bash
   npx tsx tests/t019_git_tracked_archive.test.ts
   ```
   - 结果：**100% PASS**。
3. **T018 可读归档一致性测试**:
   ```bash
   npx tsx tests/t018_readable_archive.test.ts
   ```
   - 结果：**100% PASS**。
4. **T015 报告身份订正回归**:
   ```bash
   npx tsx tests/t015_t014_report_identity_correction.test.ts
   ```
   - 结果：**100% PASS**。
5. **T013 任务总线保留检查**:
   ```bash
   npx tsx tests/t013_task_bus_preservation.test.ts
   ```
   - 结果：**100% PASS**（39 个历史任务总线记录完整无丢失）。

---

## 5. Safety, Domain, and No-Apply Compliance

- [x] 当前工作在 `agent/tree` 分支，代码提交与推送仅限 `origin/agent/tree`；
- [x] 未修改任何生产环境 `FORMATION_LIBRARY`、`public/ai-bundle.iife.js` 或线上阵型配置；
- [x] 未进行任何候选的生产 apply/deploy 操作；
- [x] 未修改任何 `TASKS/generation/**` 文件及 generation 域代码；
- [x] 完整保留所有 `TASKS/tree/` 历史记录。
