STATUS: OPEN
DOMAIN: tree

# T049R2 - Aggregate Verification-State Semantics

> T049R mathematical migration is accepted: V2 ledger rows no longer fabricate W/D/L and raw rows satisfy arithmetic invariants. One evidence-semantics defect remains: 89 `AGGREGATE_SCORE_ONLY` rows still carry `verificationState=INDEPENDENT_VERIFIED`. This task is a narrow metadata repair only.

## Defect

`AGGREGATE_SCORE_ONLY` means the source has no complete raw per-game outcome vector. It cannot be `INDEPENDENT_VERIFIED`, regardless of whether the source score was generated by a product-path aggregate run. The current contradiction affects T0 and candidate L3/L2/L1 rows and can mislead audit review.

## Required Repair

1. Preserve the V2 ledger file as append-only historical provenance. Do not delete or rewrite old rows in place.
2. Add a V3 correction/index or append superseding records with explicit state:

```text
outcomeEvidenceKind: AGGREGATE_SCORE_ONLY
verificationState: UNVERIFIED_AGGREGATE_ONLY
scoreFormula: SOURCE_AGGREGATE_UNRECONSTRUCTABLE
w/d/l/pureWinRate: null
```

3. Only records with `RAW_OUTCOMES_RECONCILED` and complete T048-compatible raw coverage may be `INDEPENDENT_VERIFIED` or `INDEPENDENT_INCONCLUSIVE`.
4. Update the audit report/checker so it fails on:

```text
AGGREGATE_SCORE_ONLY + INDEPENDENT_VERIFIED
```

5. The human-readable audit table must show aggregate score-only rows as score-only, even when the execution path was product-path.
6. Do not change the all2rush policy retest result. Preserve its actual baseline/user policy comparison and state clearly that the current user policy has no observed L3/L2 improvement in this sample; do not auto-adopt or tier-promote it.

## Required Checks

```text
V3/current index has zero aggregate-only rows marked independently verified
raw rows remain mathematically valid
all2rush baseline/user policy raw rows remain present and unchanged
no score inheritance across policy fingerprint
T048 quarantine remains authoritative
no policy/threshold/tier/source formation/web behavior change
no apply/deploy/publish
```

## Delivery

Write `TASKS/tree/T049R2.report.md` with before/after state counts, raw-row preservation, checker output, all2rush policy comparison unchanged, and no-apply confirmation. Commit/push only `agent/tree`.
