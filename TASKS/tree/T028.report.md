STATUS: PARTIAL
DOMAIN: tree

# T028 - Real Game Engine Parity Gate Report (RETRACTED & CORRECTED BY T029)

> Domain: `tree` | Executor branch: `agent/tree`
> Classification: `BYTE_PARITY_PASS_BEHAVIOR_PARITY_INVALID` | `SANDBOX_ENGINE_UNVERIFIED`
> Retraction Notice: The byte-level SHA-256 match is retained as a valid observation. The behavioral parity PASS claim is RETRACTED due to harness tautology and unpropagated side parameters identified in T029.

---

## 1. Retraction and Defect Record

严格按照 **T029（Repair T028: Independent Real-Entry Engine Parity）** 审计要求，对 T028 原始报告中的无效行为对齐结论进行正式撤回与勘误：

1. **同源自调用缺陷（Harness Tautology Defect）**：
   原 T028 测试用例在对比时连续两次调用了 Tree 域沙盒池（`pool.evalCandidateWithDeploymentTraces`），该测试仅证明了单侧 Tree Runner 的自身可重复性，未接入真实应用独立入口 `playFullGame`（`GameEngine` + `BattleSystem` + `planForRound`）。
2. **`side` 变量未透传缺陷（Unpropagated Side Defect）**：
   原测试循环中的 `side` 变量仅作为元数据记录，未实际透传给执行方法，导致所声称的双侧覆盖不实。
3. **数据重分类（Historical Evidence Reclassification）**：
   历史产物与初筛数据（T014~T027）在完成 T029 真正独立的真实入口行为对齐前，全部标记为 **`SANDBOX_ENGINE_UNVERIFIED`**，不作为生产替换或正式晋级依据。

---

## 2. Retained Valid Observation: Byte-Level SHA-256 Equality

主工作区权威真实应用 Bundle 与 Tree Runner 加载的 Bundle 在字节级 SHA-256 上保持完全一致：
- **权威路径**: `D:\develope\monsrise1\public\ai-bundle.iife.js`
- **SHA-256**: `a9821e8986b8722e89ccd32024e650e3f5853bf75df23c74a3e92e35240d6e43`
- **字节一致性**: `isByteIdentical === true`

真实入口级独立行为对齐已移交至 **T029** 彻底修复。
