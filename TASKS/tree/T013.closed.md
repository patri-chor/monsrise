STATUS: DONE
DOMAIN: tree

# T013 - Tree Task-Bus Preservation Repair Acceptance

## Decision

Accepted. Implementation commit: `2144e8a`.

## Verified

Executed from a clean tree worktree:

```bash
npx tsx tests/t013_task_bus_preservation.test.ts
```

All three checks passed:

- five historical task records and T012 report are present and non-empty;
- the deletion fixture detects unrelated task-bus file removal;
- the current task tree passes the preservation check.

The delivery changed no optimizer source under `src/**` and no generation-domain, active-library, apply, bundle, or watcher data.

## Next Gate

Task-bus preservation is now a prerequisite for future tree deliveries. The next experiment is T014: the seven representative early bundles with in-ecosystem variant/seed holdout and calculated-unit-ratio-driven order search.
