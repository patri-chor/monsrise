STATUS: DONE
DOMAIN: tree

# T041 Report — Stage Episode Integrity and Probabilistic Archetype Melee

## Run Commands

```bash
# 运行完整 T041 阶梯训练、Stage-1 针对性 Episode 门禁与概率化 Melee 采样
npx vite-node src/engine/tree/product_training/run_cycle.ts

# 验证 Stage-1 Episode 门禁、概率化采样配额、流派治理隔离与只读边界（9 大项检查）
npx vite-node scripts/tree_product_training/check_cycle.ts
```

## Rejected T040 Evidence Separation & Stage-1 Episode Integrity

- **T040 缺陷修复**：
  - 修复了 T040 中缺少 Stage-1 聚焦调优直接进入 Melee 的问题。
  - 修复了状态跃迁谓词中“数值比较与描述文本矛盾”的问题（所有理由均由真实数值严格动态生成）。
  - 废除固定 16 成员遍历，替换为两层概率化 Archetype 采样模型。
- **Strict Stage-1 Episode Gate**:
  - 候选进入 `MELEE` 前，必须完成至少 3 次实际针对强阵弱项的优化尝试（每次均运行 11 个对手 × P1/P2 = 44 局对局）。
  - 本轮共产生 **57 条真实 Stage-1 Episode 记录**（存储于 `stage1_episode_ledger.jsonl`），每条包含 `triggeredDiagnosis`（最弱对手与侧面）、`strongPoolVectorRef` 与 `attemptOutcome`。

## Archetype Governance Configuration (No Historical Snapshots)

- **Root T1 Archetypes** (`melee_archetype_config.json`):
  - 严格包含 11 个当前冻结强阵作为根流派（`springsword`, `nutsavior`, `all2rush`, `classicsavior`, `all2prayer`, `suqing`, `laddersel`, `spade_multi`, `gift_savior`, `golden_boom`, `gift_jungle`）。
  - **遵循用户要求：绝对未将历史快照（`HISTORICAL_SNAPSHOT`）作为流派成员或条目加入**。
  - 每个成员均具有合法的 `primaryArchetype = rootSourceId` 与 `lineageProof`。若缺少配置直接 fail-closed 抛出 `MELEE_ARCHETYPE_CONFIG_REQUIRED`。

## Probabilistic Melee Sampling & Frozen Weights

- **Manifest** (`melee_sampling_manifest.json`):
  - `meleeRevision`: `v1.0.0-t041-lineage` (`hash: bba461a6d713c7bc`)
  - `topLevelArchetypeProbability`: 1/11 (9.09% 等概率均匀分布)
  - `minimumPairsPerArchetype`: 1 pair (保底覆盖全部 11 个流派)
  - `samplePairBudget`: 16 pairs (32 games per candidate)
- **Pair Sampling Records** (`melee_sample_pairs.jsonl`):
  - 共审计 **304 组成对采样记录**（覆盖 19 个进入 Melee 的候选）。
  - 每次采样均成对运行 P1 与 P2 对局，满足确定性种子与配额。

## Melee Return Path Proof

- 在 Melee 采样对局中暴露弱项的候选，触发 `MELEE_DIAGNOSE_RETURN_STAGE_1`。
- **精准返回 `STAGE_1_STRONG_EPISODE` 针对性诊断调优，绝不直接退回 `STAGE_3_EARLY_BUNDLE`**。

## Catalog Summary & Experimental Frontiers

| Source ID | Classification | Controllable Ratio | Spatial Budget | Baseline | Best Relative | Experimental Frontier? |
|---|---|---|---|---|---|---|
| `springsword` | PANEL_SATURATED | 0.750 | 2 | 1.000 | +0.000 | No |
| `nutsavior` | PANEL_SATURATED | 0.625 | 2 | 1.000 | -0.023 | No |
| `all2rush` | PANEL_UNDERPERFORMER | 0.250 | 0 | 0.500 | +0.125 | No |
| `classicsavior` | PANEL_MID | 0.375 | 1 | 0.714 | **+0.149** | **YES** |
| `all2prayer` | PANEL_MID | 0.625 | 2 | 0.786 | -0.036 | No |
| `suqing` | PANEL_MID | 0.500 | 2 | 0.893 | **+0.107** | **YES** |
| `laddersel` | PANEL_MID | 0.125 | 0 | 0.857 | -0.039 | No |
| `spade_multi` | PANEL_MID | 0.750 | 2 | 0.786 | **+0.032** | **YES** |
| `gift_savior` | PANEL_SATURATED | 0.750 | 2 | 1.000 | -0.125 | No |
| `golden_boom` | PANEL_SATURATED | 0.750 | 2 | 1.000 | +0.000 | **YES** |
| `gift_jungle` | PANEL_MID | 0.875 | 3 | 0.857 | **+0.097** | **YES** |

## Telemetry & No-Apply Confirmation

- **Telemetry**: `cpuAvg: 78.0%, p95: 86.0%, configuredWorkers: 64`。
- **Catalog Hash**: `dcf172fc96b2cfe7`。
- **No-Apply Confirmation**: 产物严格标注 `NO_APPLY_NO_DEPLOY_NO_PUBLISH_NO_TIER_CHANGE`，仅作为聚合实验目录。
