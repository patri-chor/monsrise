STATUS: DONE
DOMAIN: tree

# T036 Report — Product-Path Foundation and Gift Jungle Eight-Monster Repair

## Summary

Phase-1 产品路径训练基础模块已创建，`gift_jungle` 已原地修复为精确 8 怪兽可执行源。所有验收标准通过。

---

## Changed File Map

### 新建模块文件

| 文件 | 说明 |
|---|---|
| `src/engine/tree/product_training/01_sources.ts` | 加载冻结源；gift_jungle 修复验证；源指纹 |
| `src/engine/tree/product_training/02_candidates.ts` | 候选类型和确定性候选元数据；OperatorFamily 类型；无长时间搜索 |
| `src/engine/tree/product_training/03_validate.ts` | 深度合法性、精确八怪规则、规范指纹、重复/无操作拒绝 |
| `src/engine/tree/product_training/branch_semantics.ts` | 侧感知分支选择；R1 可观察性辅助器；P1/P2 坐标语义 |
| `src/engine/tree/product_training/index.ts` | 明确公共导出 |
| `tests/t036_product_training_foundation.test.ts` | 25 项测试，全部通过 |
| `scripts/tree_product_training/check_sources.ts` | 只读源验证脚本（10 项） |
| `scripts/tree_product_training/check_architecture.ts` | 只读架构验证脚本（51 项） |

### 修改文件

| 文件 | 修改内容 |
|---|---|
| `tests/fixtures/tree/eleven_frozen_sources.json` | gift_jungle 原地修复（见下文） |

---

## Gift Jungle 原地修复 Diff/Provenance

**修复前**：
- `sourceIndex: 10`, `id: gift_jungle`
- `team.length: 7`（缺少 monster 116）
- `isLegacyBaseline: true`
- `fingerprint: 4c913570e3c9`
- R5 叶子 (n6, n11) `placement: []`

**修复后**：
- `team.length: 8`（新增 `{monsterId: 116, badgeIds: [3, 5]}`）
- `isLegacyBaseline: false`
- `fingerprint: 1f7554a84e29`（新指纹）
- R5 叶子 (n6, n11) 各新增 `{monsterId: 116, badgeIds: [3,5], x: 8, y: 1}`（p2 视角，合法普通非 calculator 控制位）
- 新增 `sourceMetadata` 修复溯源：
  ```json
  {
    "sourceId": "gift_jungle",
    "repairKind": "add_eighth_monster_only",
    "addedSlot": {"monsterId": 116, "badgeIds": [3, 5]},
    "archiveReference": "D:\\develope\\对战ai\\_archive\\阵型库\\礼物丛林1.json",
    "historicalEvidence": "PRODUCT_PATH_FORMAL_SCREEN_T035_V1 / seven-monster / retained-separate"
  }
  ```

**保留的分支结构**：两条 R1 分支（n2 主分支兜底，n7 救星优先开）完全保留，仅 R5 叶子新增 116 放置。所有 R1-R4 的坐标、徽章、action 均未改变。

**无 gift_jungle_v2**：已验证。

---

## 历史七怪 T035 证据分离确认

- T035 证据目录 `tests/fixtures/tree/experience_library/product_path_t035/` 完整保留
- 协议：`PRODUCT_PATH_FORMAL_SCREEN_T035_V1`
- manifest 哈希：`7bb394b394eba26466ec6d7ee4ed3489cd2b8fc966edda97c81452f103c13d61`（未变更）
- gift_jungle 在 T035 中为 `isLegacyBaseline=true`，未参与 T035 评估（正确缺席 source_baselines.jsonl）
- 新修复的 gift_jungle 有新指纹，与 T035 协议完全独立

---

## R1 分支证据

gift_jungle 树有 2 个 R1 分支节点（n2 空条件兜底；n7 带条件分支）。

测试验证：
- P2 侧：树坐标 `x=7` → 产品坐标 `x=7`（直接使用）
- P1 侧：树坐标 `x=7` → 产品坐标 `x=3`（镜像 10-7）
- P1 侧：树坐标 `x=9` → 产品坐标 `x=1`（镜像 10-9）
- 空敌方状态 → R1 兜底分支被选中（P1/P2 双侧）
- side-only 条件（`{side:1, main:null, subs:[], keys:[]}`）：接受，R1 可观察
- side+fullrush 条件：接受，R1 可观察
- `requiresBoardIds=true` 在 R1：拒绝（future-state）

---

## 源指纹

| sourceId | 旧指纹 | 新指纹 | isLegacyBaseline |
|---|---|---|---|
| gift_jungle | 4c913570e3c9 | 1f7554a84e29 | false（已修复） |
| 其他 10 个 | 不变 | 不变 | false |

---

## Test/Check 命令

```bash
npx vitest run tests/t036_product_training_foundation.test.ts
# 结果：25 passed (25)

npx vite-node scripts/tree_product_training/check_sources.ts
# 结果：10 passed, 0 failed ✓

npx vite-node scripts/tree_product_training/check_architecture.ts
# 结果：51 passed, 0 failed ✓
```

---

## 无仿真/无 apply 确认

本任务未运行任何对局仿真（无 PersistentSimPool 调用）。未进行任何 apply/deploy/publish/Tier 变更。所有模块均为只读类型定义和验证逻辑。
