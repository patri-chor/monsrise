STATUS: DONE
DOMAIN: tree

# T019 - T018 Git-Tracked Archive Delivery Repair Report

> Domain: `tree` | Executor branch: `agent/tree`
> Committed Archive: `tests/fixtures/tree/t016_training_archive/`
> Scope: Delivery-completeness and Git-tracking repair only. No simulation reran; no measured data changed.

---

## 1. Executive Summary

已圆满完成 **T019（T018 Git-Tracked Archive Delivery Repair）** 交付修复：
1. **完整 Git 追踪 13 项产物文件**：通过 `git add -f` 将 `tests/fixtures/tree/t016_training_archive/` 目录下的全部 13 项产物（包括 3 个 Markdown 文件 `tier_library.md`、`summary.md`、`final_r5_grids.md`）纳入 Git 跟踪；
2. **完整恢复 T018 任务规范文件**：已将 `TASKS/tree/T018-t017-readable-archive-completion.md` 原文恢复并纳入 Git 跟踪，同时完整保留 `T018.report.md`；
3. **数据一致性验证**：所有可读 Markdown 文件严格基于既有未改动的 JSON 归档数据渲染，完全对应：
   - **Tier 1 (当前基准)**: 11
   - **Tier 2 (稳定增强)**: 0
   - **Tier 3 (探索多样性)**: 5
   - **Rejected (淘汰清册)**: 25
4. **计算怪兽与遗留基准标记**：`final_r5_grids.md` 准确渲染了 11 套 Tier 1 主干网格，所有计算单位均标记 `[计算定位]`，`gift_jungle` 明确标记为 7-Monster 历史遗留基准，Tier 2 明确标注为 0 候选；
5. **Git 追踪专项测试 100% PASS**：新增 `tests/t019_git_tracked_archive.test.ts` 专项测试，直接通过 `git ls-files` 验证所有产物文件与任务规范在 Git 中的追踪状态；
6. **零重跑与 No-Apply**：未重新运行任何仿真模拟，未修改任何测得数据，未对生产环境进行任何 apply/deploy 操作。

---

## 2. Git-Tracked Archive Evidence (`git ls-files tests/fixtures/tree/t016_training_archive/`)

```text
tests/fixtures/tree/t016_training_archive/all_candidates.jsonl
tests/fixtures/tree/t016_training_archive/current_panel_generalization.jsonl
tests/fixtures/tree/t016_training_archive/early_holdout_evaluations.jsonl
tests/fixtures/tree/t016_training_archive/final_r5_grids.md
tests/fixtures/tree/t016_training_archive/generation_manifest.json
tests/fixtures/tree/t016_training_archive/optimization_attempts.jsonl
tests/fixtures/tree/t016_training_archive/reinforcement_attempts.jsonl
tests/fixtures/tree/t016_training_archive/rejection_ledger.jsonl
tests/fixtures/tree/t016_training_archive/screening_ledger.jsonl
tests/fixtures/tree/t016_training_archive/source_snapshot.json
tests/fixtures/tree/t016_training_archive/summary.md
tests/fixtures/tree/t016_training_archive/tier_library.json
tests/fixtures/tree/t016_training_archive/tier_library.md
```

- **全量 13 项产物文件均已处于 Git 跟踪状态**。

---

## 3. Restored Task Specifications & Preservation Evidence

- `TASKS/tree/T018-t017-readable-archive-completion.md`：已完整恢复并受 Git 跟踪；
- `TASKS/tree/T018.report.md`：完整保留；
- `TASKS/tree/T019-t018-git-tracked-archive-delivery.md`：规范文件完整保留；
- `TASKS/tree/T019.report.md`：当前交付报告。

---

## 4. Verification Commands & Results

1. **T019 Git 追踪归档专项测试**:
   ```bash
   npx tsx tests/t019_git_tracked_archive.test.ts
   ```
   - 结果：**100% PASS**（`git ls-files` 验证全部 13 项产物受跟踪、T018 规范已恢复、数量与 JSON 一致、网格计算定位标记准确）。
2. **T018 可读归档一致性专项测试**:
   ```bash
   npx tsx tests/t018_readable_archive.test.ts
   ```
   - 结果：**100% PASS**。
3. **T015 报告身份订正回归**:
   ```bash
   npx tsx tests/t015_t014_report_identity_correction.test.ts
   ```
   - 结果：**100% PASS**。
4. **T013 任务总线保留检查**:
   ```bash
   npx tsx tests/t013_task_bus_preservation.test.ts
   ```
   - 结果：**100% PASS**（38 个历史任务总线记录完整无丢失）。

---

## 5. Safety, Domain, and No-Apply Compliance

- [x] 当前工作在 `agent/tree` 分支，代码提交与推送仅限 `origin/agent/tree`；
- [x] 未重新运行任何仿真模拟或修改任何已测数据；
- [x] 未修改任何生产环境 `FORMATION_LIBRARY`、`public/ai-bundle.iife.js` 或线上阵型配置；
- [x] 未进行任何候选的生产 apply/deploy 操作；
- [x] 未修改任何 `TASKS/generation/**` 文件及 generation 域代码；
- [x] 完整保留所有 `TASKS/tree/` 历史记录。
