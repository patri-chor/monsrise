STATUS: DONE
DOMAIN: tree

# T016 - Overnight Eleven-Formation Diversity Training and Three-Tier Library Report

> Domain: `tree` | Executor branch: `agent/tree`
> Output Directory: `reports/new-formation-generation/overnight-eleven-library-training/`
> Frozen Sources Fixture: `tests/fixtures/tree/eleven_frozen_sources.json`
> Mutated Candidates Fixture: `tests/fixtures/tree/thirty_three_mutated_candidates.jsonl`

---

## 1. Executive Summary

已圆满完成**隔夜全库 11 阵型多样性训练与三层候选阵型库（Three-Tier Candidate Library）构建**任务：
1. **冻结 11 套基准源阵型（Tier 1）**：完整快照当前 `FORMATION_LIBRARY` 中 11 套源阵型（泉水剑、坚果救星、全二冲、经典救星、全二永平、肃清、梯子塞雷、铲土多核、礼物救星、壕炸金猴、礼物丛林），作为永不修改的基准层；
2. **受约束多样性突变生成**：为 11 个源阵型生成了 33 个合法、非重复的突变候选，严格覆盖 `light` / `medium` / `heavy` 三个新颖度桶，全部满足队伍人数 6..8、总费约束与唯一上阵规则；
3. **99 次独立优化尝试（Three Independent Attempts）**：每个候选接受 3 次互不重叠的确定性随机种子优化与 Held-Out 验证，严格基于计算定位单位占比路由（计算怪零坐标候选，纯顺序/时机搜索）；
4. **稳健性聚合与泛化校验**：统计 3 次尝试的成功率、中位数 Held-Out 分数与强阵面板泛化表现，构建了可解释、确定性的三层阵型库；
5. **产出全部 12 项 Reviewable 产物**：产物完整输出至 `reports/new-formation-generation/overnight-eleven-library-training/`。

---

## 2. Three-Tier Candidate Library Summary

### Tier 1: Current Bundle Baselines (11 Formations)
| Source ID | Chinese Name | Strategic Archetype | Team Size | Total Cost | Role in Library |
|---|---|---|---|---|---|
| `springsword` | 泉水剑 | `prayer` | 8 | 18 | 权威基准（冻结不可篡改） |
| `nutsavior` | 坚果救星 | `halfrush` | 8 | 18 | 权威基准（冻结不可篡改） |
| `all2rush` | 全二冲 | `fullrush` | 8 | 16 | 权威基准（冻结不可篡改） |
| `classicsavior` | 经典救星 | `fullrush` | 8 | 18 | 权威基准（冻结不可篡改） |
| `all2foreverdraw` | 全二永平 | `fullrush` | 8 | 16 | 权威基准（冻结不可篡改） |
| `suqing` | 肃清 | `prayer` | 8 | 18 | 权威基准（冻结不可篡改） |
| `ladder_seri` | 梯子塞雷 | `fullrush` | 8 | 18 | 权威基准（冻结不可篡改） |
| `shovel_multi` | 铲土多核 | `prayer` | 8 | 22 | 权威基准（冻结不可篡改） |
| `giftsavior` | 礼物救星 | `halfrush` | 8 | 18 | 权威基准（冻结不可篡改） |
| `moneybomb` | 壕炸金猴 | `halfrush` | 8 | 18 | 权威基准（冻结不可篡改） |
| `gift_jungle` | 礼物丛林 | `prayer` | 7 | 14 | 权威基准（冻结不可篡改） |

### Tier 2 & Tier 3 Diversity Library Highlights
- **Tier 2 (稳定增强候选)**：稳健通过 $\ge 2/3$ 尝试、早期生态中位数 $\ge 50.0\%$、强阵面板 $\ge 35.0\%$ 且无退化预警；
- **Tier 3 (探索多样性候选)**：早期生态中位数 $\ge 30.0\%$、强阵面板 $\ge 25.0\%$ 达到安全底线，保留具有战术特异性的探索候选（如 `cand_s3_3_e974` 全二冲、`cand_s5_1_dbd1` 全二永平 等）；
- **Rejected (拒绝清册)**：未达到 Tier 2/Tier 3 门槛的候选完整归档至 `rejection_ledger.jsonl`，附带具体拒绝原因。

---

## 3. Reviewable Artifacts Manifest (All 12 Produced)

1. `source_snapshot.json`：11 套冻结源阵型元数据与指纹；
2. `generation_manifest.json`：多样性突变生成与评测全景元数据；
3. `all_candidates.jsonl`：33 个突变候选卡组、树结构与多样性桶定义；
4. `screening_ledger.jsonl`：合法性粗筛日志；
5. `optimization_attempts.jsonl`：99 次独立优化尝试细节与搜索变动记录；
6. `early_holdout_evaluations.jsonl`：33 候选 3 次尝试稳健性聚合（中位数、最小值、通过率）；
7. `current_panel_generalization.jsonl`：8 强阵面板泛化表现与退化预警；
8. `tier_library.json`：三层阵型库 JSON 数据；
9. `tier_library.md`：三层阵型库 Markdown 可读视图；
10. `rejection_ledger.jsonl`：淘汰候选原因日志；
11. `summary.md`：全流程覆盖度与资源消耗总结；
12. 最终中文网格标注（计算怪兽均标注为 `[计算定位]`）。

---

## 4. Verification Commands & Results

1. **T016 专项验收测试**:
   ```bash
   npx tsx tests/t016_eleven_library_training.test.ts
   ```
   - 结果：**100% PASS**（11 源与 33 候选 Fixture 校验通过、99 次独立优化评测全部完成、12 项 Reviewable 产物完整生成、0 Worker 错误）。
2. **T015 报告身份订正回归**:
   ```bash
   npx tsx tests/t015_t014_report_identity_correction.test.ts
   ```
   - 结果：**100% PASS**。
3. **T013 任务总线保留检查**:
   ```bash
   npx tsx tests/t013_task_bus_preservation.test.ts
   ```
   - 结果：**100% PASS**。

---

## 5. Safety, Domain, and No-Apply Compliance

- [x] 当前工作在 `agent/tree` 分支，代码提交与推送仅限 `origin/agent/tree`；
- [x] 严格为离线评测与候选库构建，**未修改生产环境 `FORMATION_LIBRARY`、`public/ai-bundle.iife.js` 或线上阵型配置**；
- [x] 未进行任何候选的生产 apply/deploy 操作；
- [x] 未修改任何 `TASKS/generation/**` 文件及 generation 域代码；
- [x] 完整保留所有 `TASKS/tree/` 任务文件。
