STATUS: OPEN
DOMAIN: tree

# T031 - Repair T029 False Independent-Entry Parity

> Domain: `tree` | Executor branch: `agent/tree`
> T030 recovered tracked files successfully, but its source audit disproves T029's core claim. This is a correctness repair only. Do not run strength screening, promotion, H2H, continuous optimization, apply, deploy, or mutate formation/experience results.

## Tracked-Source Findings at `9143d93`

T029 is **not independently routed**. Both functions in `src/engine/tree/independent_real_entry_parity.ts` call the same tree adapter:

```ts
executeRealApplicationEntry(...) => playSpecVsSpec(...)
executeTreeRunnerEntry(...)      => playSpecVsSpec(...)
```

The module imports `playFullGame` but does not call it. Importing a product entry symbol is not executing it.

Other material defects:

1. Authority artifact path is `resolve('public/ai-bundle.iife.js')`, thus relative to executor cwd rather than the required explicit main-workspace absolute artifact.
2. `compareIndependentBehaviorParity()` sets `isDeploymentConsistent: true` without comparing deployment records.
3. It compares only winner/final W/L/round winner, omitting branch identity, planned/actual placement, attempt order, accepted/rejected/reason, budget before/cost/after, and round observations.
4. T029's `side` negative check only asserts `finalScore[0] !== undefined`; it does not establish that side changed an execution route or result.
5. Function-reference inequality (`executeRealApplicationEntry !== executeTreeRunnerEntry`) does not prove call-graph independence.

Therefore T029/T030 `DONE` claims are rejected. Historical T014-T027 strength data remains:

```text
SANDBOX_ENGINE_UNVERIFIED
```

## Required Implementation

### A. Explicit Authority Artifact

1. Use an explicit immutable authority artifact configuration. Default must be the absolute real artifact:

```text
D:\develope\monsrise1\public\ai-bundle.iife.js
```

or a named immutable artifact manifest generated by the real application build and carrying its source commit/build command.
2. `cwd` relative paths are prohibited for authority identity.
3. Runner invocation must receive/record the authority path and SHA-256 before worker startup, and fail closed on missing/mismatch path. Test a same-named divergent cwd bundle to prove it cannot influence authority identity.

### B. Actual Independent Product Entry

1. Use the concrete product battle entry exported by the real game, expected to be `playFullGame` or another product-owned documented public entry. Establish via source/import test that the real function is invoked.
2. The real adapter and every transitive module it imports must not import/call:

```text
src/engine/tree/arena.ts
playSpecVsSpec
PersistentSimPool
fine_grained_worker
```

3. It is insufficient to wrap or duplicate `playSpecVsSpec`. If the only existing product entry secretly delegates to tree code, report that structural blocker accurately and leave gate blocked; do not claim independent parity.

### C. Canonical Full Trace Comparison

1. Adapt each actual entry into canonical event records. Compare exact sequences for:

```text
side and formation identity
selected branch/node
round observation
planned coordinate and attempt order
actual coordinate
accepted/rejected plus reason
budget before / charged / after
round scores
final W/D/L
```

2. Do not set an equality boolean to a constant. Serialize both canonical traces and produce a first-field mismatch diagnostic.
3. Explicitly document only presentation-only excluded fields.
4. Run at least 10 executable sources × 3 held-out opponents × actual sides 1 and 2 with fixed seeds.

### D. Non-Tautological Negative Tests

Add tests proving all of these fail the gate:

1. authority artifact path override/missing/mismatch, including divergent cwd file;
2. an adapter whose source/call graph references `playSpecVsSpec` or other prohibited tree symbols;
3. injected branch, coordinate, attempt-order, acceptance/reason, and budget discrepancy;
4. side propagation: use an instrumented real entry and assert requested side reaches its product execution input; side 1/2 must produce distinguishable invocation evidence, not merely defined output;
5. a deliberately tree-delegating “real adapter” is rejected before comparison.

## Acceptance

- [ ] T029/T030 reports are amended to `PARTIAL` with this exact failure recorded.
- [ ] Authority artifact identity is absolute/immutable and proven independent of executor cwd.
- [ ] Real execution demonstrably invokes a product-owned entry with no prohibited tree transitive dependency.
- [ ] Full canonical branch/deployment/budget/outcome traces are actually compared across real side and tree side.
- [ ] All required negative controls fail as expected.
- [ ] Only then may strength evidence be reclassified; otherwise T014-T027 stays sandbox-unverified.
- [ ] No apply/deploy or optimization run.

## Delivery

Write `TASKS/tree/T031.report.md` with exact call provenance, artifact manifest, test commands and output, full matrix trace evidence, negative-control evidence, PASS/FAIL classification, and explicit training gate state. Commit/push only `agent/tree`. Do not modify this specification.
