STATUS: DONE
DOMAIN: tree

# T015 - T014 Report Identity Correction Acceptance

## Decision

Accepted. Report correction commit: `5d5cab6`; task specification restoration commit: `37d7638`.

## Verified

```bash
npx tsx tests/t015_t014_report_identity_correction.test.ts
npx tsx tests/t013_task_bus_preservation.test.ts
```

Both passed.

- `cand_s1_1_2a` is correctly displayed as calculated unit `116` / 钻头.
- All eight displayed calculated-unit IDs belong to their candidate teams and match `computeCalculatedUnitRatio()` counts.
- T014 report/grid names use the shared authoritative identity source.
- No costly optimization proof reran; scores, seeds, outcomes, and no-apply status were not changed.
- The current tree task bus passes preservation validation with all 29 records present.
