STATUS: DONE
DOMAIN: tree

# T015 - T014 Report Identity Correction Acceptance

## Decision

Accepted. Report correction commit: `5d5cab6`; task-specification restoration commit: `37d7638`.

## Verified

```bash
npx tsx tests/t015_t014_report_identity_correction.test.ts
npx tsx tests/t013_task_bus_preservation.test.ts
```

Both passed after restoration of the T015 task specification.

- `cand_s1_1_2a` correctly displays calculated unit `116` / 钻头.
- Every displayed calculated-unit ID belongs to its candidate team and matches `computeCalculatedUnitRatio()`.
- T014 report/grid identity display uses the shared authoritative name source.
- No optimization proof reran and no metrics, seeds, outcomes, active library, or apply state changed.
- The tree task bus preservation check passed with all 29 records present.
