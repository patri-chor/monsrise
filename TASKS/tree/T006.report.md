STATUS: DONE

# T006 落地报告 — 新阵型生成与评估试点流水线 (New Formation Generation Pilot)

> 落地方（Antigravity）→ 决策方（DeepSeek）
> 领域：`tree` | 分支：`agent/tree` | 任务：T006 — New Formation Generation Pilot

---

## 1. 完成内容

1. **新阵型生成试点流水线 (`src/engine/tree/new_formation_pilot.ts`)**:
   - 实现了有界的、多流派/架构（`prayer`, `halfrush`, `fullrush`）× 核心怪兽（`CORE_TABLE`）× 组合模块（`COMBO_MODULES`）的候选生成器。
   - 强化结构合法性校验（总费用 ≤ 18，槽位数 6~8，战术怪必带校验）。
   - 双重去重：基于规范化卡组键（`canonicalTeamKey`，含怪兽 ID 与排序徽章）及标准决策树指纹（`computeTreeFingerprint`），杜绝重复候选。

2. **有界终止保障 (Bounded Termination)**:
   - 设定硬性尝试上限（`maxAttempts`），当达到上限或空间耗尽时，安全输出结构化部分结果并标记 `terminatedReason: ATTEMPT_CAP_EXHAUSTED`，防止死循环。

3. **两阶段评估与资源隔离**:
   - Stage 1（Coarse pass）：采用低成本固定种子（`seedBase: 1000`, `games: 2`）快速粗筛。
   - Stage 2（Refined pass）：对满足阈值（`adScore >= 0.35`）的候选，采用独立种子（`seedBase: 9000`, `games: 6`）高精度复评。
   - 并行 Worker 严格限制在 `≤ 4`（默认 2，最大 4），满足并发约束。

4. **独立输出与环境无污染**:
   - 所有产物仅写入 `reports/new-formation-pilot/`：
     - `candidates.jsonl`（包含完整元数据、卡组、树、验证结果与两阶段评估分）
     - `summary.md`
     - `diagnostics.json`
   - 未修改 `FORMATION_LIBRARY`、`public/ai-bundle.iife.js` 或任何既有树决策与部署文件。

---

## 2. 改动文件清单

- `src/engine/tree/new_formation_pilot.ts`: 新阵型生成与两阶段评估流水线核心实现。
- `tests/new_formation_pilot.test.ts`: 自动化验收测试套件（覆盖 Dry-run、有界尝试上限、去重、多路径覆盖及库隔离）。
- `TASKS/tree/T006.report.md`: 任务落地交付报告。

---

## 3. 测试命令与结果

1. **验收测试套件运行**：
   ```bash
   npx vite-node tests/new_formation_pilot.test.ts
   ```
   - **Test 1 (Dry-run 模式测试)**：成功生成 6 个候选，覆盖 3+ 条不同流派路径，无重复卡组与树指纹，生成隔离产物（`PASS`）。
   - **Test 2 (有界尝试上限终止测试)**：在超大请求下于第 20 次尝试准时安全退出，返回 `ATTEMPT_CAP_EXHAUSTED`（`PASS`）。
   - **Test 3 (FORMATION_LIBRARY 活跃库未受污染测试)**：数据前后 100% 保持一致（`PASS`）。
   - **结果：全部 PASS**。

2. **既有生成器回归测试**：
   ```bash
   npx vite-node --script src/engine/tree/variant_generate.ts 全二冲 1
   ```
   - 正常执行并完成评估（`PASS`）。

3. **类型检查**：
   ```bash
   npx tsc --noEmit
   ```
   - 本次新增与修改的文件新增错误数：**0**。

---

## 4. 资源配置与统计指标

- **并发限制**：Worker Count = 2 (上限 4)
- **评估种子基准**：
  - Coarse Seed Base: `1000` (2 games)
  - Refined Seed Base: `9000` (6 games)
- **生成统计（示例运行）**：
  - 目标候选数：6
  - 实际生成接受数：6
  - 覆盖流派：`halfrush::all2::秒杀`、`fullrush::digger::盾炮`、`prayer::savior::范围克制祷徒` 等 6 条独立路径

---

## 5. 产出文件路径

- `reports/new-formation-pilot/candidates.jsonl`
- `reports/new-formation-pilot/summary.md`
- `reports/new-formation-pilot/diagnostics.json`

---

## 6. 确认事项

- [x] 未修改 `FORMATION_LIBRARY` 活跃库数据；
- [x] 未覆盖 `reports/optimized/`；
- [x] 报告已正确归档至 `TASKS/tree/T006.report.md`。
