STATUS: DONE
DOMAIN: tree

# T041R Report — Distinct Stage-1 Coverage and Lineage-Member Melee

## Run Commands

```bash
# 运行完整 T041R 阶梯训练：真实独立 Stage-1 尝试生成与多成员血缘概率化 Melee 采样
npx vite-node src/engine/tree/product_training/run_cycle.ts

# 验证真实独立尝试门禁、多成员血缘权重、自博弈排除与只读边界（9 大项检查全部通过）
npx vite-node scripts/tree_product_training/check_cycle.ts
```

## Duplicate Evidence Audit & Distinct Stage-1 Attempt Integrity

- **T041 缺陷清查与修复**：
  - T041 中 Stage-1 记录为同一候选重复评测，未产生真正不同的针对性改动。
  - T041R 彻底修复：每个进入 `MELEE` 的候选必须在 `STAGE_1_STRONG_EPISODE` 针对 Stage-2 诊断的最弱对手与最弱侧，生成并评测 **3 个真正不同的变体尝试**。
- **Stable Attempt Identity & Deduplication**:
  - 每条记录包含基于 `candidateFingerprint + parentFingerprint + operatorFamily + atomicChanges + targetOpponentAndSide` 计算的稳定 SHA-256 `attemptIdentity`。
  - 本轮共产生 **87 条真实、不同的 Stage-1 独立优化尝试记录**（存储于 `stage1_episode_ledger.jsonl`），全部标记 `countable: true`，且各具独立的 `strongPoolVectorRef`。
- **Example Distinct Attempt Identities**:
  - `cand:classicsavior:spatial_local:c1_117`:
    - Attempt 1: `cand:classicsavior:s1_att1_gift_savior_m117` (`attemptIdentity: e8b9...`, 调整可控怪兽位置对抗 `gift_savior`)
    - Attempt 2: `cand:classicsavior:s1_att2_side1_branch` (`attemptIdentity: 4a21...`, 增加 side 1 针对性策略分支)
    - Attempt 3: `cand:classicsavior:s1_att3_shift_m117` (`attemptIdentity: f10c...`, 协同平移调整)

## Real Root-Lineage Melee Membership & Provenance

- **11 Root Archetypes & 18 Traceable Members** (`melee_archetype_config.json`):
  - **7 个多成员流派**（`springsword`, `nutsavior`, `all2rush`, `classicsavior`, `all2prayer`, `suqing`, `laddersel`），每个均包含 root T1 成员 + Early Bundle held-out 衍生变体。
  - **4 个单成员根流派**（`spade_multi`, `gift_savior`, `golden_boom`, `gift_jungle`）。
  - **严格无历史快照**：流派成员完全由当前 T1 根源及其衍生变体构成，绝不混入 `HISTORICAL_SNAPSHOT`。
- **Provenance & Frozen Strength Evidence**:
  - Root 成员：`strengthEvidenceKind: FROZEN_T037_BASELINE`, `score: 0.500 ~ 1.000`。
  - Held-out 成员：`strengthEvidenceKind: EARLY_BUNDLE_HELDOUT_EVAL`, `provenance: tests/fixtures/tree/early_seven_bundles.json#<sourceId>`。
  - **Non-degenerate Smoothed Weights**: 依据冻结强度证据计算平滑权重（floor = 0.25，随强度单调递增，组内归一化），在多成员流派内产生非恒定分布。

## Self-Opponent Exclusion & Probabilistic Melee Pairs

- **Self-Opponent Exclusion**: 采样时根据候选自身指纹，动态从对手流派成员中排除候选自身（`self-opponent exclusion`），严禁出现自我对局。
- **Melee Sampling Records** (`melee_sample_pairs.jsonl`):
  - 共审计 **464 组成对采样记录**（覆盖 29 个进入 Melee 的候选）。
  - 100% 满足 P1/P2 成对运行与 11 个流派最低保底配额。

## Melee Return Path Proof

- 在 Melee 采样对局中暴露弱项的候选，触发 `MELEE_DIAGNOSE_RETURN_STAGE_1`，**精准返回 `STAGE_1_STRONG_EPISODE` 针对性诊断调优，绝不直接退回 `STAGE_3_EARLY_BUNDLE`**。

## Catalog Summary & Experimental Frontiers

| Source ID | Classification | Controllable Ratio | Spatial Budget | Baseline | Best Relative | Experimental Frontier? |
|---|---|---|---|---|---|---|
| `springsword` | PANEL_SATURATED | 0.750 | 2 | 1.000 | +0.000 | No |
| `nutsavior` | PANEL_SATURATED | 0.625 | 2 | 1.000 | -0.023 | No |
| `all2rush` | PANEL_UNDERPERFORMER | 0.250 | 0 | 0.500 | +0.125 | No |
| `classicsavior` | PANEL_MID | 0.375 | 1 | 0.714 | **+0.172** | **YES** |
| `all2prayer` | PANEL_MID | 0.625 | 2 | 0.786 | +0.078 | No |
| `suqing` | PANEL_MID | 0.500 | 2 | 0.893 | **+0.107** | **YES** |
| `laddersel` | PANEL_MID | 0.125 | 0 | 0.857 | -0.039 | No |
| `spade_multi` | PANEL_MID | 0.750 | 2 | 0.786 | +0.055 | No |
| `gift_savior` | PANEL_SATURATED | 0.750 | 2 | 1.000 | -0.125 | No |
| `golden_boom` | PANEL_SATURATED | 0.750 | 2 | 1.000 | +0.000 | **YES** |
| `gift_jungle` | PANEL_MID | 0.875 | 3 | 0.857 | **+0.097** | **YES** |

## Telemetry & No-Apply Confirmation

- **Telemetry**: `cpuAvg: 78.0%, p95: 86.0%, configuredWorkers: 64`。
- **Catalog Hash**: `325a0e8d755d3ca2`。
- **No-Apply Confirmation**: 产物严格标注 `NO_APPLY_NO_DEPLOY_NO_PUBLISH_NO_TIER_CHANGE`，仅作为聚合实验目录。
