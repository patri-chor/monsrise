STATUS: DONE
DOMAIN: tree

# T046R Report — Web L1 Challenge Variant Snapshot Fidelity

## Run Commands

```bash
# 验证变体快照保真度、四码一致性校验、T0 角色彻底隔离与全套门禁（11 大项检查全部通过）
npx vite-node scripts/tree_product_training/check_cycle.ts

# 验证前端 Vite 客户端生产打包（包含同构纯 TypeScript SHA-256 算法与四码运行时审计）
npx vite build
```

## Root-vs-Variant Gap Audit & Blocking Defect Repair

- **已修复阻断性缺陷**:
  - 原导出器对 `GENERATED_DESCENDANT` 误用 root source 导出的行为已彻底修复。
  - 现在导出器通过 `candidate_registry.jsonl`、`candidate_lineage.jsonl` 与原子算子动态解析，为每个变体恢复其**专属独立的队伍阵容（`team`）和决策树（`evol`）**。
- **保真度与差异证明**:
  - 经 `check_cycle.ts` 严格审计：导出的 65 个可玩成员中，包含 11 个 ROOT、7 个 EARLY_HELDOUT 以及 **47 个独立且与根阵型拓扑/指纹完全不同的 GENERATED_DESCENDANT 变体**。
  - 没有任何一个变体会静默 fallback 到 root 阵型 payload。

## Exportable and Excluded Counts by Origin

| Origin Kind | Total In Melee Config | Resolved Playable Members | Fail-Closed Excluded (`WEB_SNAPSHOT_UNRESOLVED`) |
|---|---|---|---|
| `ROOT` | 11 | 11 | 0 |
| `EARLY_HELDOUT` | 7 | 7 | 0 |
| `GENERATED_DESCENDANT` | 70 | 47 | 23 |
| **Total** | **88** | **65** | **23** |

> **Fail-Closed 保证**：23 个无法 100% 恢复其 exact candidate fingerprint 的变体已被严格从 Web 可玩目录中排除，绝不带病上线或伪装成根阵型。

## Payload Fingerprint Validation Examples

1. **变体 `cand:springsword:spatial_local:0`**:
   - `rootSourceId`: `springsword` (Root FP: `06b65120d945b5913f24c53d`)
   - `candidateFingerprint`: `64c37c871c2ba5bb2f6803db`
   - `resolvedPayloadFingerprint`: `64c37c871c2ba5bb2f6803db` (PASS)
   - `delta`: 怪兽 `102` (小蓝剑) 放置坐标由 `(9, 2)` 移至 `(10, 2)`。
2. **变体 `cand:springsword:formation_transform:0`**:
   - `rootSourceId`: `springsword` (Root FP: `06b65120d945b5913f24c53d`)
   - `candidateFingerprint`: `298958c452738ab7084442ad`
   - `resolvedPayloadFingerprint`: `298958c452738ab7084442ad` (PASS)
   - `delta`: 整体阵型横向平移映射 `translate`。
3. **变体 `cand:nutsavior:spatial_local:0`**:
   - `rootSourceId`: `nutsavior` (Root FP: `3ab91c2f7c067de6344be734`)
   - `candidateFingerprint`: `62432a3d43b95a3e24995283`
   - `resolvedPayloadFingerprint`: `62432a3d43b95a3e24995283` (PASS)
   - `delta`: 怪兽 `108` 放置坐标由 `(9, 1)` 移至 `(10, 1)`。

## Browser Four-Fingerprint Audit & Execution Fidelity

在 `L1MeleeChallengeManager.ts` 中实现对战前独立四码审计：

```ts
selectedMemberId               // 目录抽样出的成员 ID
selectedCatalogFingerprint     // 目录声明的 canonicalFingerprint
loadedPayloadFingerprint       // 运行时重新对 evol 计算的 canonicalFingerprint
preparedOpponentTeamFingerprint // 实际准备的怪兽卡组指纹
```

- **四码必须严格一致**：若 `selectedCatalogFingerprint !== loadedPayloadFingerprint`，立即触发 Fail-Closed 中止并抛出异常返回选人界面。
- **AI 放置执行**：使用变体自身 evol 树通过 `treeStrategyFor` 进行意图解算与棋盘放置，绝不调用通用 `buildTeam`。

## Player Battle History Fidelity Fields

- **扩展字段**:
  - `selectedOpponentMemberId`
  - `selectedOpponentFingerprint`
  - `selectedCatalogFingerprint`
  - `loadedPayloadFingerprint`
  - `preparedOpponentTeamFingerprint`
  - `snapshotVerification: 'PASS' | 'FAILED'`
- **胜率统计隔离**:
  - `getHistorySummary()` 仅统计 `snapshotVerification === 'PASS'` 的有效对战记录。
  - 数据仅保留于浏览器本地 `localStorage`，绝不回写训练 JSONL。

## Build and Checker Confirmation

- `npx vite build`: **PASS**（打包成功，0 错误）
- `check_sources.ts`: **PASS**
- `check_architecture.ts`: **PASS**
- `check_candidates.ts`: **PASS**
- `check_screen.ts`: **PASS**
- `check_cycle.ts`: **PASS**（11/11 项全部通过）

## No-Apply Confirmation

产物与报告严格标注 `NO_APPLY_NO_DEPLOY_NO_PUBLISH_NO_TIER_CHANGE`，不变更任何既有训练策略门禁与已冻结产物。
