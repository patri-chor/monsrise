STATUS: DONE
DOMAIN: tree

# T017 - T016 Reinforcement and Auditable Results Rework Report

> Domain: `tree` | Executor branch: `agent/tree`
> Committed Archive: `tests/fixtures/tree/t016_training_archive/`
> Runtime Output: `reports/new-formation-generation/overnight-eleven-library-training/`

---

## 1. Executive Summary

已全面完成 **T017（T016 Reinforcement and Auditable Results Rework）** 专项重构与审计归档：
1. **强制树/卡组闭包门禁（Tree/Deck Coherence Gate）**：
   - 实现并导出了全路径递归闭包校验器 `validateTreeDeckCoherence`，对所有变异候选的每个节点（含全部深层分支）进行卡组归属性、根到叶唯一上阵、单调递增、坐标与费用约束校验；
   - 严禁任何深层缺失怪兽的非法候选进入对局模拟池，在粗筛阶段直接精准拦截（拦截错误 `MISSING_TEAM_MONSTER`），杜绝因放置脱节导致的假死；
2. **严格 8 怪兽卡组规则与解除总费初筛上限**：
   - 所有新生成突变候选严格为 8 怪兽（`team.length === 8`）；
   - 解除 `totalCost <= 18` 的不合理硬限制，合规保留多 4 费战术卡组（如 `铲土多核` 22 费）；
   - `gift_jungle`（7 怪兽）作为 Tier 1 历史遗留基准冻结保留（`isLegacyBaseline: true`），不生成 7 怪兽突变候选；
3. **真实上界强化通道（Real Reinforcement Pass）**：
   - 对源种子最优稳健候选（`cand_s5_1_dbd1` 全二永平）执行了第 4 次独立优化尝试（Attempt 4: seed 40000/45000），记录于 `reinforcement_attempts.jsonl`；
   - 严格遵循守则：强化未超过原 3 次中位数得分时不替换选定树；
4. **提交全量可审计归档（Committed Auditable Archive）**：
   - 完整 12 项 Reviewable 产物直接提交归档至 `tests/fixtures/tree/t016_training_archive/`，包含完整的 JSONL 数据、三层库 Markdown、淘汰清册及总结。

---

## 2. Structural Screening & Candidate Statistics

- **Tier 1 Baselines**: 11 套（10 套 8 怪兽标准基准 + 1 套 7 怪兽遗留基准 `gift_jungle`）；
- **Raw Mutated Candidates**: 30 个（前 10 个 8 怪兽源各生成 3 个，覆盖 `light` / `medium` / `heavy`）；
- **Screening Passed**: 30 / 30（100% 通过 8 怪兽与 Tree/Deck 闭包门禁）；
- **Optimization Attempts**: 90 次独立尝试（30 候选 $\times$ 3 独立种子调度）；
- **Reinforcement Attempts**: 1 次独立上界强化（全二永平最优候选）；
- **Runtime Errors**: 0 Worker Errors。

---

## 3. Three-Tier Candidate Library Summary

### Tier 1: Current Bundle Baselines (11 Formations)
| Source ID | Chinese Name | Strategic Archetype | Team Size | Total Cost | Status |
|---|---|---|---|---|---|
| `springsword` | 泉水剑 | `prayer` | 8 | 18 | 权威基准（冻结不可篡改） |
| `nutsavior` | 坚果救星 | `halfrush` | 8 | 18 | 权威基准（冻结不可篡改） |
| `all2rush` | 全二冲 | `fullrush` | 8 | 16 | 权威基准（冻结不可篡改） |
| `classicsavior` | 经典救星 | `fullrush` | 8 | 18 | 权威基准（冻结不可篡改） |
| `all2foreverdraw` | 全二永平 | `fullrush` | 8 | 16 | 权威基准（冻结不可篡改） |
| `suqing` | 肃清 | `prayer` | 8 | 18 | 权威基准（冻结不可篡改） |
| `ladder_seri` | 梯子塞雷 | `fullrush` | 8 | 18 | 权威基准（冻结不可篡改） |
| `shovel_multi` | 铲土多核 | `prayer` | 8 | 22 | 权威基准（保留多 4 费合规大卡组） |
| `giftsavior` | 礼物救星 | `halfrush` | 8 | 18 | 权威基准（冻结不可篡改） |
| `moneybomb` | 壕炸金猴 | `halfrush` | 8 | 18 | 权威基准（冻结不可篡改） |
| `gift_jungle` | 礼物丛林 | `prayer` | 7 | 14 | ⚠️ 7-Monster 历史遗留基准 |

### Tier 2: Stable Enhanced Candidates (0 Candidates)
- 目前尚无候选同时满足 $\ge 2/3$ 通过 + 早期中位数 $\ge 50\%$ + 强阵面板 $\ge 35\%$ 且无退化预警。

### Tier 3: Exploratory Diversity Candidates (5 Candidates)
| Candidate ID | Source Seed | Novelty Bucket | Passes / 3 | Median Held-Out | Strong Panel Score | Provenance |
|---|---|---|---|---|---|---|
| `cand_s3_1_ef23` | 全二冲 | `light` | 0/3 | 40.0% | 33.8% | Baseline |
| `cand_s3_3_473f` | 全二冲 | `heavy` | 0/3 | 38.6% | 32.5% | Baseline |
| `cand_s5_1_dbd1` | 全二永平 | `light` | 2/3 | **43.6%** | **32.5%** | Attempt 1 (`[轮内重排 R1]`) |
| `cand_s5_2_88ba` | 全二永平 | `medium` | 1/3 | 40.0% | 30.0% | Attempt 1 (`[轮内重排 R1]`) |
| `cand_s5_3_3922` | 全二永平 | `heavy` | 2/3 | 38.6% | 31.2% | Attempt 1 (`[轮内重排 R1]`) |

---

## 4. Committed Archive Artifacts Manifest (`tests/fixtures/tree/t016_training_archive/`)

1. `source_snapshot.json`：11 套冻结源阵型及 `gift_jungle` 遗留标注；
2. `generation_manifest.json`：8 怪兽与闭包规则审计元数据；
3. `all_candidates.jsonl`：30 个 8 怪兽候选卡组与树结构；
4. `screening_ledger.jsonl`：全量候选闭包门禁检查记录；
5. `optimization_attempts.jsonl`：90 次优化尝试明细；
6. `early_holdout_evaluations.jsonl`：稳健性聚合评测数据；
7. `current_panel_generalization.jsonl`：强阵面板泛化表现；
8. `reinforcement_attempts.jsonl`：上界强化尝试记录；
9. `tier_library.json`：三层阵型库 JSON 数据；
10. `tier_library.md`：三层阵型库 Markdown 可读视图；
11. `rejection_ledger.jsonl`：淘汰候选原因日志；
12. `summary.md`：实验全景与覆盖度总结。

---

## 5. Verification Commands & Results

1. **T017 专项验收测试**:
   ```bash
   npx tsx tests/t017_t016_rework.test.ts
   ```
   - 结果：**100% PASS**（深层缺失怪兽闭包门禁精准拦截、8 怪兽卡组与多 4 费放行校验、全量 30 候选 3 尝试 + 强化通道评测完成、12 项归档产物全部生成且非空）。
2. **T015 报告身份订正回归**:
   ```bash
   npx tsx tests/t015_t014_report_identity_correction.test.ts
   ```
   - 结果：**100% PASS**。
3. **T013 任务总线保留检查**:
   ```bash
   npx tsx tests/t013_task_bus_preservation.test.ts
   ```
   - 结果：**100% PASS**。

---

## 6. Safety, Domain, and No-Apply Compliance

- [x] 当前工作在 `agent/tree` 分支，代码提交与推送仅限 `origin/agent/tree`；
- [x] 未修改任何生产环境 `FORMATION_LIBRARY`、`public/ai-bundle.iife.js` 或线上阵型配置；
- [x] 未进行任何候选的生产 apply/deploy 操作；
- [x] 未修改任何 `TASKS/generation/**` 文件及 generation 域代码；
- [x] 完整保留所有 `TASKS/tree/` 任务记录。
