STATUS: DONE
DOMAIN: tree

# T036R Report — Product Adapter R1 Branch Parity Repair

## Summary

已通过直接调用 `treeStrategyFor(formationToEvol(gift_jungle))(ctx)` 建立完整的 R1 产品适配器证据，覆盖 P1/P2 双侧、n2 兜底与 n7/fullrush 分支。所有断言基于真实 branchId 和坐标，而非辅助函数。

---

## 无需修改 n7 分支条件

n7 label `"对方全冲可能救星开"` 经 `labelToMask` 转译为 `{side:null, main:'fullrush', subs:[], keys:[]}`。  
`fullrush` 由 `recognizeArchetype` 在对手手牌含 ≥2 个 `[114,113,107]` 时触发 — **完全 R1 可观察**（handIds）。  
无需修改分支条件、放置坐标、卡组或徽章。

---

## 触发输入与识别结果

| 场景 | 输入 | `recognizeArchetype` 结果 | 选中节点 |
|---|---|---|---|
| 兜底 | `enemyRevealedHand=[]` | `main=null` | n2 (`条件空 mask`) |
| fullrush | `enemyRevealedHand=[{114},{113}]` | `main=fullrush` | n7 (`main=fullrush`) |

---

## 期望/实际 branchId 与意图（每侧）

### P2 侧 (side=2)，树坐标直接使用

| 触发 | branchId | 意图 m110 plannedX | 意图 m110 plannedY |
|---|---|---|---|
| 兜底 (n2) | `n2` ✓ | 7 (treeX=7) ✓ | 3 ✓ |
| fullrush (n7) | `n7` ✓ | 7 (treeX=7) ✓ | 3 ✓ |

### P1 侧 (side=1)，x → 10-x 镜像

| 触发 | branchId | 意图 m110 plannedX | 意图 m110 plannedY |
|---|---|---|---|
| 兜底 (n2) | `n2` ✓ | 3 (10-7) ✓ | 3 ✓ |
| fullrush (n7) | `n7` ✓ | 3 (10-7) ✓ | 3 ✓ |

---

## 验证命令

```bash
npx vitest run tests/t036_product_training_foundation.test.ts
# 结果：30 passed (30)（含原始 25 + 5 新直接适配器测试）

npx vite-node scripts/tree_product_training/check_sources.ts
# 结果：10 passed, 0 failed ✓

npx vite-node scripts/tree_product_training/check_architecture.ts
# 结果：52 passed, 0 failed ✓（含新 R1 直接适配器断言 7 项）
```

---

## 辅助函数与产品适配器一致性

`branch_semantics.getR1BranchSelection()` 与 `treeStrategyFor()(ctx)` 对同一输入（fullrush P2 侧）均选中 `n7`，已通过一致性测试。

---

## 无仿真/无 apply 确认

本任务未运行任何 PersistentSimPool / playFullGame 批量仿真。未进行任何 apply/deploy/publish/Tier 变更。仅构造轻量 `DeploymentStrategyContext` 调用策略函数，纯确定性计算。
