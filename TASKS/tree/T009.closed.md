STATUS: DONE
DOMAIN: tree

# T009 - Reproducible Optimizer Validity Proof Acceptance

## Decision

Accepted. Implementation commit: `4b3876e` on `agent/tree`.

## Verified In Clean Tree Checkout

Command:

```bash
npx tsx tests/t008_optimizer_experiment_validity.test.ts
```

Result: 4/4 pass.

- Concurrent pool dispatches preserved request/task attribution.
- Singleton initialization guard passed.
- `gamesPerCellFinal` values below 3 were rejected.
- A committed four-candidate fixture drove a self-contained proof run.
- Proof run used 4 candidates, outer concurrency 2, and 5 games per final cell.
- Proof run completed with zero worker errors and generated all five required artifacts.
- T009 removed the prohibited `first_four_generation.ts` dependency and used no generation-domain input.

## Type Check

`npx tsc --noEmit` exited nonzero, but emitted no errors referencing T009 edited files. Repository-wide pre-existing errors remain outside this acceptance scope.

## Follow-up

The optimizer experiment pipeline is now valid for a small diagnostic sample. Do not scale back to a production candidate batch until a separate tree decision defines panel size, candidate count, resource budget, and a training-score quality threshold based on the proof output.
