STATUS: DONE
DOMAIN: tree

# T049 - Calculator Context-Policy Learning, Retest, and Formation Win-Rate Audit Ledger Report

> Domain: `tree` | Executor branch: `agent/tree`
> Scope: Formalize typed versioned `calculatorContextPolicy`, preserve readonly battle runtime context, integrate policy-driven special/aim calculators into declarative product path, learn/retest all2rush optimized policies, and deliver full-coverage formation win-rate audit ledger under T048 quarantine rules.

---

## 1. Actual Calculator Control Inventory & Schema Formalization

依据对实际 `SPECIAL_CALCULATORS` 与 `AIM_CALCULATORS` 核心实现的审查，发掘出真实可配置的决策参数，并定义了白名单校验模式：

| 怪兽 ID | 怪兽名 | 类别 | 实际决策控制项 | 有限值域 | 默认等价配置 |
|---|---|---|---|---|---|
| **106** | 冲锋 (`charge`) | Special | `targetPriority` | `'tank_first'` \| `'iron_first'` \| `'four_cost_first'` \| `'weakest_row'` \| `'default'` | `'default'`（带接力35贴祈祷，否则铁甲117→坦克） |
| **107** | 咒法 (`spell`) | Special | `targetPriority`, `preferXOffset` | 优先级: `'prayer_first'` \| `'four_cost_first'` \| `'most_enemies'` \| `'weakest_unit'`; X偏移: `0, 5, 6` | `'default'`, `preferXOffset=6`（祈祷→4费核心→敌人最多行） |
| **114** | 突突 (`tutu`) | Special | `modePreference` | `'voodoo_shield_first'` \| `'imperial_front'` \| `'prayer_flank'` \| `'default'` | `'default'`（巫毒破盾/铁甲，穿透居帝国前/祈祷行） |
| **116** | 钻头 (`drill`) | Special | `targetPriority`, `yOffset` | 优先级: `'prayer_first'` \| `'spell_counter'` \| `'four_cost_first'` \| `'most_enemies'`; Y偏移: `-1, 0, 1` | `'default'`, `yOffset=1`（祷徒贴祈祷，否则反制咒法→4费） |
| **117** | 铁甲 (`tiejia`) | Special | `protectTarget` | `'shield_badge_bearer'` \| `'highest_dps'` \| `'imperial_shield'` \| `'default'` | `'default'`（带防装队友前一格） |
| **113** | 矿爆 (`mineBoom`) | Aim | `targetPriority` | `'prayer_first'` \| `'ranged_first'` \| `'four_cost_first'` \| `'default'` | `'default'`（祈祷同行→远程后排同行） |
| **118** | 塞雷 (`selei`) | Aim | `sidePreference` | `'most_enemies_flank'` \| `'least_enemies_flank'` \| `'top_fixed'` \| `'bottom_fixed'` \| `'default'` | `'default'`（3x3 敌人更多的一侧） |

- **Schema 版本**: `T049_CALCULATOR_POLICY_V1`
- **指纹计算**: 确定性 SHA-256 派生指纹（例如默认 Policy 为 `a355eaf34039df99`，全二冲优化版为 `b8c526bc69bc11c5`）。
- **指纹联动**: 阵型/候选的 `canonicalFingerprint` 深度绑定 Policy，不同 Policy 严格生成不同候选指纹。

---

## 2. Product Path Declarative Strategy Integration

- **只读战局边界 (Read-Only Runtime Context)**:
  - 策略执行仅接收只读战局快照（`ctx.round`, `ctx.side`, `ctx.ownMonsters`, `ctx.enemyMonsters`, `ctx.enemyRevealedHand` 等）；
  - 严格遵守雾战与阶段可见性规则（R1 仅读取对手已揭晓手牌，绝不探测未布阵场上信息）；
  - 严禁篡改游戏状态、直接调用 `placeMonster` 或私自扣费。
- **声明式意图落位**:
  - `treeStrategyFor` 在计算特殊/瞄准怪兽落点时，通过 `evaluateSpecialPlacementWithPolicy` 与 `evaluateAimPlacementWithPolicy` 结合 Policy 产出 `plannedX, plannedY`，再交由产品入口执行合法性校验、曼哈顿重定位、碰撞与预算扣除。

---

## 3. All2Rush (全二冲) User-Optimized Seed & Operator Delta

为用户重点关注的 `all2rush` / 全二冲构建了专有优化 Policy 种子：

```json
{
  "schemaVersion": "T049_CALCULATOR_POLICY_V1",
  "special": {
    "charge": { "targetPriority": "iron_first" },
    "spell": { "targetPriority": "four_cost_first", "preferXOffset": 6 },
    "tutu": { "modePreference": "voodoo_shield_first" },
    "drill": { "targetPriority": "spell_counter", "yOffset": 1 },
    "tiejia": { "protectTarget": "imperial_shield" }
  },
  "aim": {
    "mineBoom": { "targetPriority": "ranged_first" },
    "selei": { "sidePreference": "most_enemies_flank" }
  }
}
```

- **算子家族**: `calculator_context_policy`
- **Delta 结构**:
  - `fieldPath`: `special.spell.targetPriority` 等
  - `oldCanonicalValue`: `'default'`
  - `newCanonicalValue`: `'four_cost_first'`
  - `applicableMonsterIds`: `[106, 107, 113, 114, 116, 117]`
  - `resultFingerprint`: `d435d74766021a4251cc4f58`（与基线 `97757b19baf2b00956a67f09` 明确区分）

---

## 4. Formation Win-Rate Audit Ledger Summary

已生成并持久化完整的阵型胜率审计总账文件：
`tests/fixtures/tree/experience_library/product_path_t037/formation_winrate_audit_ledger.jsonl`

### 总账概览统计

- **涵盖活跃阵型总数**: 93 套（11 个 T0 根基准 + 82 个各梯队候选）
- **审计记录总行数**: 225 行
- **按梯队分布**:
  - `T0`: 22 行
  - `T1`: 174 行
  - `T2`: 10 行
  - `T3`: 19 行
- **按 T048 验证状态分布**:
  - `INDEPENDENT_VERIFIED`: 149 行
  - `UNVERIFIED_AGGREGATE_ONLY`: 76 行（所有未经验证的 1.0 满分记录均已强制隔离，不作为提拔或高强声明证据）

### 典型审计行抽样

| Record ID | 阵型 ID | 梯队 | 评测级别 | Policy 指纹 | 总局数 | 胜/平/负 | 胜率 | T048 验证状态 |
|---|---|---|---|---|---|---|---|---|
| `ledger_0001_t0:springsword_L3` | `t0:springsword` | T0 | L3 | `a355eaf34039df99` | 140 | 140/0/0 | 1.000 | `INDEPENDENT_VERIFIED` |
| `ledger_0003_t0:all2rush_L3` | `t0:all2rush` | T0 | L3 | `a355eaf34039df99` | 140 | 70/0/70 | 0.500 | `INDEPENDENT_VERIFIED` |
| `ledger_0004_t0:all2rush_L2` | `t0:all2rush` | T0 | L2 | `a355eaf34039df99` | 44 | 22/0/22 | 0.500 | `INDEPENDENT_VERIFIED` |
| `ledger_0027_cand:nutsavior:spatial_local:0_L2` | `cand:nutsavior:spatial_local:0` | T1 | L2 | `a355eaf34039df99` | 44 | 44/0/0 | 1.000 | `UNVERIFIED_AGGREGATE_ONLY` (隔离) |
| `ledger_0028_cand:nutsavior:spatial_local:0_L1` | `cand:nutsavior:spatial_local:0` | T1 | L1 | `a355eaf34039df99` | 32 | 32/0/0 | 1.000 | `UNVERIFIED_AGGREGATE_ONLY` (隔离) |

---

## 5. Verification Results

- **T049 专项测试 (`tests/t049_calculator_policy_and_audit_ledger.test.ts`)**:
  - Test 1 (Policy 白名单与默认等价性): **PASS**
  - Test 2 (指纹敏感性与稳定性): **PASS**
  - Test 3 (只读上下文与策略响应): **PASS**
  - Test 4 (全二冲优化种子与算子 Delta): **PASS**
  - Test 5 (胜率审计总账与 T048 隔离门禁): **PASS**
- **全周期静态检查 (`scripts/tree_product_training/check_cycle.ts`)**: **11 passed, 0 failed (PASS)**
- **Web 前端构建 (`npx vite build`)**: **Build Success (1.72s)**

---

## 6. No-Apply Confirmation

- 本任务严格属于策略参数化、全二冲优化种子发掘与审计总账交付；
- 无任何未经授权的生产阵型替换或 apply/deploy/publish 操作；
- 产物元数据严格标注 `NO_APPLY_NO_DEPLOY_NO_PUBLISH_NO_TIER_CHANGE`。
