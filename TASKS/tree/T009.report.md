STATUS: DONE
DOMAIN: tree

# T009 - Reproducible Optimizer Validity Proof Report

> Domain: `tree` | Executor branch: `agent/tree`

## 1. Summary

本报告是对 T008 验收重构任务（T009）的完整交付。
已经彻底消除了对 generation 域文件和忽略产物（`reports/new-formation-generation/first-four-cycle/generated_candidates.jsonl`）的外部依赖，并在 clean tree checkout 下实现 100% 独立可复现验证：
1. **移除 generation 源码与算子**：删除了误引入的 `src/engine/tree/first_four_generation.ts`，Tree 域自身通过自洽的 `resolveEvaluationPanel` 解析评测面板，绝不触碰任何 mutation/generation 算子；
2. **确定性测试 Fixture**：在 `tests/fixtures/tree/four_frozen_candidates.jsonl` 中提交了仅包含树优化所需 4 个有效候选数据的测试固定集；
3. **Clean Checkout 独立可复现 Proof Run**：测试套件直接基于 committed fixture 运行，并在干净检出环境中完整生成 `reports/new-formation-generation/optimizer-validity-proof/` 全部 5 个产物；
4. **全套 T008 核心功能保持 100% 通过**：并发 pool 隔离、`initPromise` 单例防重入、`gamesPerCellFinal < 3` 强拦截、`trainingScore` 全链路决策驱动与 6 种明确 terminal outcomes 均正常生效且 0 worker errors。

---

## 2. Fixture & Changed Files

### Committed Test Fixture
- `tests/fixtures/tree/four_frozen_candidates.jsonl`: 包含 4 个有效 frozen candidate 结构（仅含卡组与基础树结构，不依赖任何生成算子）。

### Modified / Deleted Files
- `[DELETED]` `src/engine/tree/first_four_generation.ts`: 彻底移除 generation 模块。
- `[MODIFIED]` `src/engine/tree/candidate_optimization_runner.ts`: 移除对 `first_four_generation` 的引用，内部提供 `resolveEvaluationPanel()`。
- `[MODIFIED]` `src/engine/tree/sequential_tree_optimization.ts`: 移除对 `first_four_generation` 的引用，优先加载 `tests/fixtures/tree/four_frozen_candidates.jsonl` 作为基准。
- `[MODIFIED]` `tests/t008_optimizer_experiment_validity.test.ts`: 显式传入 committed fixture 路径，确保在 clean checkout 环境下无外部依赖。

---

## 3. Test Verification & Proof Run Evidence

### Clean-Checkout Execution Command
```bash
npx tsx tests/t008_optimizer_experiment_validity.test.ts
```

### Test Suite Results: 4 / 4 PASS (100%)
- `[Test 1]` `PersistentSimPool` 并发请求隔离与 requestId 路由安全 -> **PASS**
- `[Test 2]` `init()` 单例并发防重复初始化 -> **PASS**
- `[Test 3]` `gamesPerCellFinal < 3` 严格配置错误抛出与拦截 -> **PASS**
- `[Test 4]` 诊断 Proof Run (4 候选, outer workers=2, 5 局/格) -> **PASS**

### Proof Run Generated Artifacts
- `reports/new-formation-generation/optimizer-validity-proof/panel_manifest.json` (604 B)
- `reports/new-formation-generation/optimizer-validity-proof/optimization_results.jsonl` (8,279 B)
- `reports/new-formation-generation/optimizer-validity-proof/independent_final_evaluation.jsonl` (23,661 B)
- `reports/new-formation-generation/optimizer-validity-proof/quality_decision.json` (1,105 B)
- `reports/new-formation-generation/optimizer-validity-proof/summary.md` (1,887 B)

### Detailed Outcome Counts
| Outcome Category | Count | Status Description |
|---|---|---|
| `IMPROVED` | 2 | 验证集增益 $\ge 5\%$ 且负场不恶化 (`cand_s1_1_2a`, `cand_s1_2_2b`) |
| `NO_INFORMATIVE_SPLIT` | 0 | - |
| `NO_OBSERVED_TRIGGER_AT_FORK` | 0 | - |
| `BRANCH_SEARCH_NO_TRAINING_GAIN` | 1 | 搜索空间未获得训练分增益 (`cand_s1_3_2c`) |
| `VALIDATION_TRAINING_REJECTED` | 1 | 独立验证集未达到增益门限或负场增加 (`cand_s2_1_8e`) |
| `ERROR` | 0 | 0 Worker 崩溃与 0 undefined 访问 |

---

## 4. Safety & Domain Compliance Confirmation

- [x] 未修改任何 `TASKS/generation/**` 或 generation 域源码；
- [x] 移除了误引入的 `src/engine/tree/first_four_generation.ts`；
- [x] 未修改 active `FORMATION_LIBRARY`、`public/ai-bundle.iife.js` 或线上阵型库；
- [x] 未修改 `scripts/watch-gemini.ps1`；
- [x] 未将任何未达标候选应用到生产环境中；
- [x] 所有交付物严格提交推送至 `agent/tree` 分支。
