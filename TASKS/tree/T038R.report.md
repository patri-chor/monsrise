STATUS: DONE
DOMAIN: tree

# T038R Report — Resumable Adaptive Loop Activation and Aggregate-Mode Boundary Repair

## Run Commands

```bash
# 执行自适应循环（含 cycle 0 初始演化、cycle 1 跨周期状态链接与幂等性验证）
npx vite-node src/engine/tree/product_training/run_cycle.ts

# 验证周期、剪枝、幂等性、去重与只读目录（14 项只读检查）
npx vite-node scripts/tree_product_training/check_cycle.ts
```

## Cycle Identities, Parent Links & Idempotent Rerun Proof

- **Cycle 0**:
  - `cycleId`: `03e91129486c`
  - `cycleOrdinal`: `0`
  - `parentCycleId`: `null`
  - `parentCatalogHash`: `null`
  - `sourcesProcessed`: 11
- **Cycle 1**:
  - `cycleId`: `ac9bbcfce2d2`
  - `cycleOrdinal`: `1`
  - `parentCycleId`: `03e91129486c`
  - `parentCatalogHash`: `bcbd68ef2d349369`
  - `sourcesProcessed`: 11
- **Idempotency Proof (No-op rerun)**:
  - 再次以相同输入调用 `executeCycle({ cycleOrdinal: 1 })` 时，命中幂等性检查：
    `[IDEMPOTENT NO-OP] Cycle ac9bbcfce2d2 (ordinal 1) is already fully completed.`
  - 判定为 `isNoOp: true`，未写入任何重复 decision / prune / catalog 记录。

## Duplicate Records Audit

- `t038_cycle_decisions.jsonl`：包含 22 条唯一记录（Cycle 0 的 11 个源 + Cycle 1 的 11 个源），按 `recordId` 与 `(cycleId, sourceId)` 唯一性校验 **0 重复**。
- `t038_prune_trials.jsonl`：包含 13 条唯一后剪枝试验记录，按 `recordId` 唯一性校验 **0 重复**。

## Adaptive Candidate Production & Operator Counts

- **实际生成的自适应候选**：
  - `spatial_local`：基于可控性空间预算分配，低可控源（`springsword`, `spade_multi`, `gift_savior`, `golden_boom`, `gift_jungle`）预算为 0 并记录原因。
  - `formation_transform`：生成合法的 `flip_vertical` 变换。
  - `strategy_schedule_branch`：真实生成跨回合 R1 side-aware 分支候选（例如 `cand:*:strategy_schedule_branch:c*_side2`），满足 R1 敌方手牌/徽章可观察性约束。
  - `multi_monster_exploration`：针对弱源（`all2rush`）及连续未改进达到阈值源，由确定性随机种子生成包含 2-4 个协调合法原子变更（如 `move_placement` 协同位移）的候选，并完整记录 `parentFingerprint`, `rollbackParentFingerprint`, `atomicChanges`, `escalationReason`, `failedSingleOperatorCount`。

## Aggregate Labels & Boundary Conformity

- 所有记录严格执行边界修饰，不含 `Promoted`, `Promotion`, `Tier-ready` 等批准性词汇：
  - `runtime_candidate_catalog.json`：
    - `evidenceClass`: `"AGGREGATE_EXPLORATION_ONLY"`
    - `integrationStatus`: `"EXPERIMENTAL_UNVERIFIED_NOT_FOR_AUTO_INTEGRATION"`
    - `formalPromotionStatus`: `"NOT_EVALUATED"`
    - `experimentalFrontierCount`: `7`（彻底替代 `promotionCount`）
    - `isExperimentalFrontier`: `boolean`（彻底替代 `isPromotion`）
  - `t038_cycle_decisions.jsonl`：所有决策行包含 `evidenceClass: "AGGREGATE_EXPLORATION_ONLY"`
  - `t038_prune_trials.jsonl`：所有后剪枝行包含 `evidenceClass: "AGGREGATE_EXPLORATION_ONLY"` 与 `heuristicStatus: "AGGREGATE_HEURISTIC_UNVERIFIED"`

## Post-Pruning Heuristic Records

- 对 7 个实验性前沿候选进行了产品路径贪心后剪枝（基于容差 `PRUNE_REGRESSION_TOLERANCE = 0.05` 与 `PRUNE_WEAKEST_SIDE_TOLERANCE = 0.07`）。
- 测试的条件分支在移除后均产生不可接受回归，全数保留（决策 `KEPT`），无 material regression。

## Source Frontiers (Catalog Summary)

| Source ID | Maturity | Ctrl Ratio | Spatial Budget | Baseline Score | Best Relative | Experimental Frontier? | Branches Pruned |
|---|---|---|---|---|---|---|---|
| `springsword` | STRONG | 0.25 | 0 (LOW_CTRL) | 1.000 | +0.000 | No | 0 |
| `nutsavior` | STRONG | 0.38 | 1 | 1.000 | +0.000 | No | 0 |
| `all2rush` | WEAK | 0.75 | 2 | 0.500 | **+0.071** | **YES** | 0 |
| `classicsavior` | MID | 0.63 | 2 | 0.714 | **+0.250** | **YES** | 0 |
| `all2prayer` | MID | 0.38 | 1 | 0.786 | **+0.107** | **YES** | 0 |
| `suqing` | MID | 0.50 | 2 | 0.893 | **+0.107** | **YES** | 0 |
| `laddersel` | MID | 0.88 | 3 | 0.857 | **+0.071** | **YES** | 0 |
| `spade_multi` | MID | 0.25 | 0 (LOW_CTRL) | 0.786 | **+0.071** | **YES** | 0 |
| `gift_savior` | STRONG | 0.25 | 0 (LOW_CTRL) | 1.000 | +0.000 | No | 0 |
| `golden_boom` | STRONG | 0.25 | 0 (LOW_CTRL) | 1.000 | +0.000 | No | 0 |
| `gift_jungle` | MID | 0.14 | 0 (LOW_CTRL) | 0.857 | **+0.143** | **YES** | 0 |

## Catalog Schema Diff

```diff
- schemaVersion: 'T038_CATALOG_V1'
- promotionCount: 4
- isPromotion: true
+ schemaVersion: 'T038_CATALOG_V1'
+ evidenceClass: 'AGGREGATE_EXPLORATION_ONLY'
+ integrationStatus: 'EXPERIMENTAL_UNVERIFIED_NOT_FOR_AUTO_INTEGRATION'
+ formalPromotionStatus: 'NOT_EVALUATED'
+ experimentalFrontierCount: 7
+ isExperimentalFrontier: true
```

## Concurrency & No-Apply Confirmation

- `run_cycle.ts` 外部并发 <=2，内部 `PersistentSimPool` 采用滑动窗口动态控制。
- 产物为只读 catalog（哈希 `bcbd68ef2d349369`），完全遵守 `NO_APPLY_NO_DEPLOY_NO_PUBLISH_NO_TIER_CHANGE`。
